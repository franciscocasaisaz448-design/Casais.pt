-- ⚡ CASAIS SCRIPTER - Admin Panel PRO ⚡
-- 🎯 Script Organizado e Otimizado

-- Safety check for Roblox environment
if not game or not game:GetService then
    warn("This script must be run in a Roblox environment")
    return
end

-- ═══════════════════════════════════════════════════════════════
--                          SERVIÇOS
-- ═══════════════════════════════════════════════════════════════

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

-- Verificações de compatibilidade do executor
local hasDrawing = pcall(function() return Drawing end)
local hasVIM = pcall(function() return game:GetService("VirtualInputManager") end)
if not hasDrawing then print("Aviso: Biblioteca Drawing não disponível. ESP boxes e tracers desabilitados.") end
if not hasVIM then print("Aviso: VirtualInputManager não disponível. Auto clicker pode não funcionar.") end

-- Função para simular clique do mouse (necessária para o auto clicker)
local function mouse1click()
    if not hasVIM then return end
    local vim = game:GetService("VirtualInputManager")
    local mousePos = UserInputService:GetMouseLocation()
    vim:SendMouseButtonEvent(mousePos.X, mousePos.Y, 0, true, game, 0)
    wait(0.03)
    vim:SendMouseButtonEvent(mousePos.X, mousePos.Y, 0, false, game, 0)
end

-- ═══════════════════════════════════════════════════════════════
--                         VARIÁVEIS GLOBAIS
-- ═══════════════════════════════════════════════════════════════

local selectedPlayer = nil
local playerButtons = {}
local spectating = false
local prevMouseBehavior
local prevMouseIconEnabled
local panelVisible = false

-- ═══════════════════════════════════════════════════════════════
--                        SISTEMA DE VOO
-- ═══════════════════════════════════════════════════════════════

_G.FlySystem = {
    flying = false,
    noclip = false,
    speed = 50,
    bodyGyro = nil,
    bodyVelocity = nil,
    control = {F=0, B=0, L=0, R=0, U=0, D=0}
}

function _G.FlySystem.toggleFly()
    _G.FlySystem.flying = not _G.FlySystem.flying
    local char = LocalPlayer.Character
    if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end

    if _G.FlySystem.flying then
        _G.FlySystem.bodyGyro = Instance.new("BodyGyro", hrp)
        _G.FlySystem.bodyGyro.MaxTorque = Vector3.new(4e5, 4e5, 4e5)
        _G.FlySystem.bodyGyro.CFrame = hrp.CFrame
        _G.FlySystem.bodyVelocity = Instance.new("BodyVelocity", hrp)
        _G.FlySystem.bodyVelocity.MaxForce = Vector3.new(4e5, 4e5, 4e5)
        _G.FlySystem.bodyVelocity.Velocity = Vector3.zero
    else
        if _G.FlySystem.bodyGyro then _G.FlySystem.bodyGyro:Destroy() end
        if _G.FlySystem.bodyVelocity then _G.FlySystem.bodyVelocity:Destroy() end
    end
end

function _G.FlySystem.disableFly()
    _G.FlySystem.flying = false
    if _G.FlySystem.bodyGyro then _G.FlySystem.bodyGyro:Destroy() end
    if _G.FlySystem.bodyVelocity then _G.FlySystem.bodyVelocity:Destroy() end
end

function _G.FlySystem.toggleNoclip()
    _G.FlySystem.noclip = not _G.FlySystem.noclip
end

function _G.FlySystem.init()
    -- Controles de entrada
    UserInputService.InputBegan:Connect(function(input, gpe)
        if gpe then return end
        if input.KeyCode == Enum.KeyCode.W then _G.FlySystem.control.F = 1
        elseif input.KeyCode == Enum.KeyCode.S then _G.FlySystem.control.B = 1
        elseif input.KeyCode == Enum.KeyCode.A then _G.FlySystem.control.L = 1
        elseif input.KeyCode == Enum.KeyCode.D then _G.FlySystem.control.R = 1
        elseif input.KeyCode == Enum.KeyCode.Space then _G.FlySystem.control.U = 1
        elseif input.KeyCode == Enum.KeyCode.LeftControl then _G.FlySystem.control.D = 1 end
    end)

    UserInputService.InputEnded:Connect(function(input, gpe)
        if gpe then return end
        if input.KeyCode == Enum.KeyCode.W then _G.FlySystem.control.F = 0
        elseif input.KeyCode == Enum.KeyCode.S then _G.FlySystem.control.B = 0
        elseif input.KeyCode == Enum.KeyCode.A then _G.FlySystem.control.L = 0
        elseif input.KeyCode == Enum.KeyCode.D then _G.FlySystem.control.R = 0
        elseif input.KeyCode == Enum.KeyCode.Space then _G.FlySystem.control.U = 0
        elseif input.KeyCode == Enum.KeyCode.LeftControl then _G.FlySystem.control.D = 0 end
    end)

    -- Loop principal do sistema de voo
    RunService.RenderStepped:Connect(function()
        local char = LocalPlayer.Character
        if not char then return end
        local hrp = char:FindFirstChild("HumanoidRootPart")
        if not hrp then return end

        -- Noclip
        if _G.FlySystem.noclip then
            for _, v in pairs(char:GetDescendants()) do
                if v:IsA("BasePart") and v.CanCollide then 
                    v.CanCollide = false 
                end
            end
        end

        -- Sistema de voo
        if _G.FlySystem.flying and hrp then
            local cam = workspace.CurrentCamera
            local moveVec = Vector3.zero
            moveVec = moveVec + cam.CFrame.LookVector * (_G.FlySystem.control.F - _G.FlySystem.control.B)
            moveVec = moveVec + cam.CFrame.RightVector * (_G.FlySystem.control.R - _G.FlySystem.control.L)
            moveVec = moveVec + Vector3.new(0,1,0) * (_G.FlySystem.control.U - _G.FlySystem.control.D)
            if moveVec.Magnitude > 0 then
                hrp.CFrame = hrp.CFrame + moveVec.Unit * _G.FlySystem.speed
            end
        end
    end)
end

-- ═══════════════════════════════════════════════════════════════
--                      FUNÇÕES DE JOGADORES
-- ═══════════════════════════════════════════════════════════════

function instantTPTo(player)
    if player and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
        local char = LocalPlayer.Character
        if char and char:FindFirstChild("HumanoidRootPart") then
            char.HumanoidRootPart.CFrame = player.Character.HumanoidRootPart.CFrame
        end
    end
end

function startSpectate(player)
    if player and player.Character and player.Character:FindFirstChild("Humanoid") then
        spectating = player
        Camera.CameraSubject = player.Character.Humanoid
    end
end

function stopSpectate()
    spectating = false
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        Camera.CameraSubject = LocalPlayer.Character.Humanoid
    end
end

-- ═══════════════════════════════════════════════════════════════
--                      INTERFACE PRINCIPAL
-- ═══════════════════════════════════════════════════════════════

local gui = Instance.new("ScreenGui")
gui.Name = "AdminPanel"
gui.ResetOnSpawn = false
gui.Parent = LocalPlayer:WaitForChild("PlayerGui")

-- Frame principal
local frame = Instance.new("Frame")
frame.Size = UDim2.fromOffset(600, 400)
frame.Position = UDim2.new(0.5, -300, 0.5, -200)
frame.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
frame.Active = true
frame.Draggable = true
frame.Visible = false
frame.Parent = gui
Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 15)

-- Cabeçalho
local header = Instance.new("Frame")
header.Size = UDim2.new(1, 0, 0, 50)
header.BackgroundColor3 = Color3.fromRGB(139, 92, 246)
header.Parent = frame
Instance.new("UICorner", header).CornerRadius = UDim.new(0, 15)

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -60, 1, 0)
title.Position = UDim2.new(0, 10, 0, 0)
title.BackgroundTransparency = 1
title.Font = Enum.Font.GothamBold
title.TextSize = 20
title.TextColor3 = Color3.new(1, 1, 1)
title.TextXAlignment = Enum.TextXAlignment.Left
title.Text = "⚡ CASAIS SCRIPTER PRO"
title.Parent = header

local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.fromOffset(40, 40)
closeBtn.Position = UDim2.new(1, -45, 0, 5)
closeBtn.Text = "✕"
closeBtn.Font = Enum.Font.GothamBold
closeBtn.TextSize = 18
closeBtn.BackgroundColor3 = Color3.fromRGB(239, 68, 68)
closeBtn.TextColor3 = Color3.new(1, 1, 1)
closeBtn.Parent = header
Instance.new("UICorner", closeBtn).CornerRadius = UDim.new(0, 8)

-- ═══════════════════════════════════════════════════════════════
--                        SISTEMA DE ABAS
-- ═══════════════════════════════════════════════════════════════

-- Container das abas
local tabs = Instance.new("ScrollingFrame")
tabs.Size = UDim2.new(0, 150, 1, -95)
tabs.Position = UDim2.new(0, 15, 0, 85)
tabs.BackgroundTransparency = 1
tabs.BorderSizePixel = 0
tabs.ScrollBarThickness = 4
tabs.CanvasSize = UDim2.new(0, 0, 0, 0)
tabs.AutomaticCanvasSize = Enum.AutomaticSize.Y
tabs.Parent = frame

local tabsLayout = Instance.new("UIListLayout", tabs)
tabsLayout.Padding = UDim.new(0, 8)
tabsLayout.SortOrder = Enum.SortOrder.LayoutOrder

-- Função para criar abas
local function makeTab(text, order)
    local b = Instance.new("TextButton")
    b.Size = UDim2.new(1, -8, 0, 50)
    b.Text = text
    b.Font = Enum.Font.GothamBold
    b.TextSize = 14
    b.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
    b.TextColor3 = Color3.fromRGB(180, 180, 190)
    b.LayoutOrder = order
    b.Parent = tabs
    
    local corner = Instance.new("UICorner", b)
    corner.CornerRadius = UDim.new(0, 12)
    
    local stroke = Instance.new("UIStroke", b)
    stroke.Color = Color3.fromRGB(50, 50, 60)
    stroke.Thickness = 1
    stroke.Transparency = 0.5
    
    return b
end

-- Criando as abas
local tabPlayers = makeTab("👥 JOGADORES", 1)
local tabMovement = makeTab("🚀 MOVIMENTO", 2)
local tabAimbot = makeTab("🎯 AIMBOT", 3)
local tabClicker = makeTab("🖱️ CLICKER", 4)
local tabVision = makeTab("👁️ VISÃO", 5)
local tabMisc = makeTab("⚙️ MISC", 6)

-- Container das páginas
local pages = Instance.new("Frame")
pages.Size = UDim2.new(1, -185, 1, -95)
pages.Position = UDim2.new(0, 175, 0, 85)
pages.BackgroundTransparency = 1
pages.Parent = frame

-- Função para criar páginas
local function makePage()
    local p = Instance.new("ScrollingFrame")
    p.Size = UDim2.fromScale(1, 1)
    p.BackgroundTransparency = 1
    p.BorderSizePixel = 0
    p.ScrollBarThickness = 6
    p.CanvasSize = UDim2.new(0, 0, 0, 0)
    p.AutomaticCanvasSize = Enum.AutomaticSize.Y
    p.Visible = false
    p.Parent = pages
    
    local layout = Instance.new("UIListLayout", p)
    layout.Padding = UDim.new(0, 10)
    layout.SortOrder = Enum.SortOrder.LayoutOrder
    
    return p
end

-- Criando as páginas
local playersPage = makePage()
local movementPage = makePage()
local aimbotPage = makePage()
local clickerPage = makePage()
local visionPage = makePage()
local miscPage = makePage()

-- Definir página inicial
playersPage.Visible = true

-- ═══════════════════════════════════════════════════════════════
--                      SISTEMA ESP
-- ═══════════════════════════════════════════════════════════════

local espEnabled = false
local espBoxEnabled = false
local espMaxDistance = 1000
local espHighlights = {}
local espBoxes = {}
local tracersEnabled = false
local tracersList = {}
local nametagsEnabled = false
local nametagsGuis = {}

local function getPlayerDistance(player)
    if not LocalPlayer.Character or not player.Character then return math.huge end
    local myPos = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    local theirPos = player.Character:FindFirstChild("HumanoidRootPart")
    if not myPos or not theirPos then return math.huge end
    return (myPos.Position - theirPos.Position).Magnitude
end

local function createESPBox(player)
    if not Drawing then return end
    
    local box = {
        topLeft = Drawing.new("Line"),
        topRight = Drawing.new("Line"),
        bottomLeft = Drawing.new("Line"),
        bottomRight = Drawing.new("Line")
    }
    
    for _, line in pairs(box) do
        line.Thickness = 2
        line.Color = Color3.fromRGB(0, 255, 0)
        line.Transparency = 1
        line.Visible = false
    end
    
    return box
end

local function updateESPBox(player, box)
    if not player.Character or not box then return end
    
    local char = player.Character
    local hrp = char:FindFirstChild("HumanoidRootPart")
    local head = char:FindFirstChild("Head")
    
    if not hrp or not head then
        for _, line in pairs(box) do
            line.Visible = false
        end
        return
    end
    
    local distance = getPlayerDistance(player)
    if distance > espMaxDistance then
        for _, line in pairs(box) do
            line.Visible = false
        end
        return
    end
    
    local headPos, headOnScreen = Camera:WorldToViewportPoint(head.Position + Vector3.new(0, 0.5, 0))
    local rootPos, rootOnScreen = Camera:WorldToViewportPoint(hrp.Position - Vector3.new(0, 3, 0))
    
    if headOnScreen and rootOnScreen and headPos.Z > 0 and rootPos.Z > 0 then
        local height = math.abs(headPos.Y - rootPos.Y)
        local width = height * 0.6
        
        local x1 = headPos.X - width/2
        local x2 = headPos.X + width/2
        local y1 = headPos.Y
        local y2 = rootPos.Y
        
        -- Top line
        box.topLeft.From = Vector2.new(x1, y1)
        box.topLeft.To = Vector2.new(x2, y1)
        
        -- Bottom line
        box.bottomLeft.From = Vector2.new(x1, y2)
        box.bottomLeft.To = Vector2.new(x2, y2)
        
        -- Left line
        box.topRight.From = Vector2.new(x1, y1)
        box.topRight.To = Vector2.new(x1, y2)
        
        -- Right line
        box.bottomRight.From = Vector2.new(x2, y1)
        box.bottomRight.To = Vector2.new(x2, y2)
        
        for _, line in pairs(box) do
            line.Visible = espBoxEnabled and espEnabled
        end
    else
        for _, line in pairs(box) do
            line.Visible = false
        end
    end
end

local function updateESP()
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            local distance = getPlayerDistance(player)
            if distance <= espMaxDistance then
                local char = player.Character
                local highlight = espHighlights[player]
                
                if not highlight then
                    highlight = Instance.new("Highlight")
                    highlight.Adornee = char
                    highlight.FillTransparency = 0.5
                    highlight.OutlineTransparency = 0
                    highlight.Parent = char
                    espHighlights[player] = highlight
                end
                
                -- Cor baseada na distância
                if distance < 50 then
                    highlight.FillColor = Color3.fromRGB(255, 0, 0)
                    highlight.OutlineColor = Color3.fromRGB(255, 0, 0)
                elseif distance < 200 then
                    highlight.FillColor = Color3.fromRGB(255, 255, 0)
                    highlight.OutlineColor = Color3.fromRGB(255, 255, 0)
                else
                    highlight.FillColor = Color3.fromRGB(0, 255, 0)
                    highlight.OutlineColor = Color3.fromRGB(0, 255, 0)
                end
                
                highlight.Enabled = espEnabled
                
                -- Atualizar box ESP
                if not espBoxes[player] then
                    espBoxes[player] = createESPBox(player)
                end
                updateESPBox(player, espBoxes[player])
            else
                -- Remover ESP se muito longe
                if espHighlights[player] then
                    espHighlights[player]:Destroy()
                    espHighlights[player] = nil
                end
                if espBoxes[player] then
                    for _, line in pairs(espBoxes[player]) do
                        line:Remove()
                    end
                    espBoxes[player] = nil
                end
            end
        end
    end
end

-- ═══════════════════════════════════════════════════════════════
--                      SISTEMA TRACERS
-- ═══════════════════════════════════════════════════════════════

local function updateTracers()
    -- Limpar tracers antigos
    for _, line in pairs(tracersList) do
        if line then line:Remove() end
    end
    tracersList = {}
    
    if not tracersEnabled or not Drawing then return end
    
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            local distance = getPlayerDistance(player)
            if distance <= espMaxDistance then
                local hrp = player.Character:FindFirstChild("HumanoidRootPart")
                if hrp then
                    local screenPos, onScreen = Camera:WorldToViewportPoint(hrp.Position)
                    if onScreen and screenPos.Z > 0 then
                        pcall(function()
                            local line = Drawing.new("Line")
                            line.From = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y)
                            line.To = Vector2.new(screenPos.X, screenPos.Y)
                            
                            -- Cor baseada na distância
                            if distance < 50 then
                                line.Color = Color3.fromRGB(255, 0, 0)
                            elseif distance < 200 then
                                line.Color = Color3.fromRGB(255, 255, 0)
                            else
                                line.Color = Color3.fromRGB(0, 255, 0)
                            end
                            
                            line.Thickness = 2
                            line.Transparency = 1
                            line.Visible = true
                            table.insert(tracersList, line)
                        end)
                    end
                end
            end
        end
    end
end

-- ═══════════════════════════════════════════════════════════════
--                      SISTEMA NAMETAGS
-- ═══════════════════════════════════════════════════════════════

local function updateNametags()
    -- Limpar nametags antigos
    for player, gui in pairs(nametagsGuis) do
        if gui then gui:Destroy() end
    end
    nametagsGuis = {}
    
    if not nametagsEnabled then return end
    
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            local distance = getPlayerDistance(player)
            if distance <= espMaxDistance then
                local head = player.Character:FindFirstChild("Head")
                local humanoid = player.Character:FindFirstChildOfClass("Humanoid")
                if head and humanoid then
                    local billboard = Instance.new("BillboardGui")
                    billboard.Adornee = head
                    billboard.Size = UDim2.new(0, 200, 0, 80)
                    billboard.StudsOffset = Vector3.new(0, 2, 0)
                    billboard.AlwaysOnTop = true
                    billboard.Parent = head

                    local nameLabel = Instance.new("TextLabel")
                    nameLabel.Size = UDim2.new(1, 0, 0.4, 0)
                    nameLabel.BackgroundTransparency = 1
                    nameLabel.Text = player.Name
                    nameLabel.Font = Enum.Font.GothamBold
                    nameLabel.TextSize = 14
                    nameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
                    nameLabel.TextStrokeTransparency = 0.5
                    nameLabel.Parent = billboard
                    
                    local distanceLabel = Instance.new("TextLabel")
                    distanceLabel.Size = UDim2.new(1, 0, 0.3, 0)
                    distanceLabel.Position = UDim2.new(0, 0, 0.4, 0)
                    distanceLabel.BackgroundTransparency = 1
                    distanceLabel.Text = math.floor(distance) .. "m"
                    distanceLabel.Font = Enum.Font.Gotham
                    distanceLabel.TextSize = 12
                    distanceLabel.TextColor3 = Color3.fromRGB(255, 255, 0)
                    distanceLabel.TextStrokeTransparency = 0.5
                    distanceLabel.Parent = billboard
                    
                    local healthLabel = Instance.new("TextLabel")
                    healthLabel.Size = UDim2.new(1, 0, 0.3, 0)
                    healthLabel.Position = UDim2.new(0, 0, 0.7, 0)
                    healthLabel.BackgroundTransparency = 1
                    healthLabel.Text = math.floor(humanoid.Health) .. "/" .. math.floor(humanoid.MaxHealth)
                    healthLabel.Font = Enum.Font.Gotham
                    healthLabel.TextSize = 12
                    healthLabel.TextColor3 = Color3.fromRGB(0, 255, 0)
                    healthLabel.TextStrokeTransparency = 0.5
                    healthLabel.Parent = billboard

                    nametagsGuis[player] = billboard
                end
            end
        end
    end
end

-- ═══════════════════════════════════════════════════════════════
--                      VARIÁVEIS DO AIMBOT
-- ═══════════════════════════════════════════════════════════════

local aimbotEnabled = false
local aimbotFOV = 80
local aimbotTarget = "Head"
local aimbotKey = Enum.UserInputType.MouseButton2
local aimbotKeyName = "Botão Direito"
local aimbotSmooth = 5
local fovCircle = nil
local waitingForKey = false

-- Criar círculo FOV se Drawing estiver disponível
if Drawing then
    pcall(function()
        fovCircle = Drawing.new("Circle")
        fovCircle.Thickness = 2
        fovCircle.NumSides = 50
        fovCircle.Filled = false
        fovCircle.Transparency = 1
        fovCircle.Color = Color3.fromRGB(255, 70, 70)
    end)
end

-- ═══════════════════════════════════════════════════════════════
--                   VARIÁVEIS DO AUTO CLICKER
-- ═══════════════════════════════════════════════════════════════

local autoClickerEnabled = false
local autoClickerSpeed = 50
local autoClickerKey = Enum.KeyCode.C
local autoClickerKeyName = "C"
local waitingForClickerKey = false
local lastClickTime = 0

-- ═══════════════════════════════════════════════════════════════
--                      FUNÇÕES DO AIMBOT
-- ═══════════════════════════════════════════════════════════════

local function getClosestPlayer()
    local closestPlayer = nil
    local shortestDistance = aimbotFOV
    local mousePos = UserInputService:GetMouseLocation()

    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            local char = player.Character
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum and hum.Health > 0 then
                local targetPart = char:FindFirstChild(aimbotTarget)
                if targetPart then
                    local screenPos, onScreen = Camera:WorldToViewportPoint(targetPart.Position)
                    if onScreen and screenPos.Z > 0 then
                        local distance = (Vector2.new(screenPos.X, screenPos.Y) - mousePos).Magnitude
                        if distance < shortestDistance then
                            shortestDistance = distance
                            closestPlayer = player
                        end
                    end
                end
            end
        end
    end

    return closestPlayer
end

-- ═══════════════════════════════════════════════════════════════
--                      LOOPS PRINCIPAIS
-- ═══════════════════════════════════════════════════════════════

-- Loop do Aimbot
RunService.RenderStepped:Connect(function()
    pcall(function()
        if aimbotEnabled then
            -- Atualizar círculo FOV
            if fovCircle then
                local mousePos = UserInputService:GetMouseLocation()
                fovCircle.Position = mousePos
                fovCircle.Radius = aimbotFOV
                fovCircle.Visible = true
            end

            -- Verificar se tecla está pressionada
            local isKeyPressed = false
            if aimbotKey == Enum.UserInputType.MouseButton2 then
                isKeyPressed = UserInputService:IsMouseButtonPressed(Enum.UserInputType.MouseButton2)
            elseif aimbotKey == Enum.UserInputType.MouseButton1 then
                isKeyPressed = UserInputService:IsMouseButtonPressed(Enum.UserInputType.MouseButton1)
            else
                isKeyPressed = UserInputService:IsKeyDown(aimbotKey)
            end

            -- Mirar no alvo
            if isKeyPressed then
                local target = getClosestPlayer()
                if target and target.Character then
                    local targetPart = target.Character:FindFirstChild(aimbotTarget)
                    if targetPart then
                        local targetPos = targetPart.Position
                        local currentCFrame = Camera.CFrame
                        local targetCFrame = CFrame.new(currentCFrame.Position, targetPos)
                        local smoothValue = math.max(aimbotSmooth, 1)
                        Camera.CFrame = currentCFrame:Lerp(targetCFrame, 1 / smoothValue)
                    end
                end
            end
        else
            if fovCircle then
                fovCircle.Visible = false
            end
        end
    end)
end)

-- Loop do Auto Clicker
RunService.Heartbeat:Connect(function()
    if autoClickerEnabled then
        local currentTime = tick()
        if currentTime - lastClickTime >= autoClickerSpeed / 1000 then
            pcall(function() mouse1click() end)
            lastClickTime = currentTime
        end
    end
end)

-- ═══════════════════════════════════════════════════════════════
--                    SISTEMA DE NAVEGAÇÃO
-- ═══════════════════════════════════════════════════════════════

local function setTab(which)
    -- Resetar todas as abas
    tabPlayers.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
    tabPlayers.TextColor3 = Color3.fromRGB(180, 180, 190)
    tabMovement.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
    tabMovement.TextColor3 = Color3.fromRGB(180, 180, 190)
    tabAimbot.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
    tabAimbot.TextColor3 = Color3.fromRGB(180, 180, 190)
    tabClicker.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
    tabClicker.TextColor3 = Color3.fromRGB(180, 180, 190)
    tabVision.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
    tabVision.TextColor3 = Color3.fromRGB(180, 180, 190)
    tabMisc.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
    tabMisc.TextColor3 = Color3.fromRGB(180, 180, 190)
    
    -- Ocultar todas as páginas
    playersPage.Visible = false
    movementPage.Visible = false
    aimbotPage.Visible = false
    clickerPage.Visible = false
    visionPage.Visible = false
    miscPage.Visible = false
    
    -- Ativar aba selecionada
    if which == "PLAYERS" then 
        tabPlayers.BackgroundColor3 = Color3.fromRGB(139, 92, 246)
        tabPlayers.TextColor3 = Color3.fromRGB(255, 255, 255)
        playersPage.Visible = true
    elseif which == "MOVEMENT" then 
        tabMovement.BackgroundColor3 = Color3.fromRGB(139, 92, 246)
        tabMovement.TextColor3 = Color3.fromRGB(255, 255, 255)
        movementPage.Visible = true
    elseif which == "AIMBOT" then 
        tabAimbot.BackgroundColor3 = Color3.fromRGB(139, 92, 246)
        tabAimbot.TextColor3 = Color3.fromRGB(255, 255, 255)
        aimbotPage.Visible = true
    elseif which == "CLICKER" then 
        tabClicker.BackgroundColor3 = Color3.fromRGB(139, 92, 246)
        tabClicker.TextColor3 = Color3.fromRGB(255, 255, 255)
        clickerPage.Visible = true
    elseif which == "VISION" then 
        tabVision.BackgroundColor3 = Color3.fromRGB(139, 92, 246)
        tabVision.TextColor3 = Color3.fromRGB(255, 255, 255)
        visionPage.Visible = true
    else 
        tabMisc.BackgroundColor3 = Color3.fromRGB(139, 92, 246)
        tabMisc.TextColor3 = Color3.fromRGB(255, 255, 255)
        miscPage.Visible = true 
    end
end

-- Conectar eventos das abas
tabPlayers.MouseButton1Click:Connect(function() setTab("PLAYERS") end)
tabMovement.MouseButton1Click:Connect(function() setTab("MOVEMENT") end)
tabAimbot.MouseButton1Click:Connect(function() setTab("AIMBOT") end)
tabClicker.MouseButton1Click:Connect(function() setTab("CLICKER") end)
tabVision.MouseButton1Click:Connect(function() setTab("VISION") end)
tabMisc.MouseButton1Click:Connect(function() setTab("MISC") end)

-- ═══════════════════════════════════════════════════════════════
--                    CONTROLE DO PAINEL
-- ═══════════════════════════════════════════════════════════════

local function showPanel(show)
    if panelVisible == show then return end
    panelVisible = show
    frame.Visible = show
    
    if show then
        prevMouseBehavior = UserInputService.MouseBehavior
        prevMouseIconEnabled = UserInputService.MouseIconEnabled
        UserInputService.MouseBehavior = Enum.MouseBehavior.Default
        UserInputService.MouseIconEnabled = true
    else
        if prevMouseBehavior then 
            UserInputService.MouseBehavior = prevMouseBehavior 
        end
        if prevMouseIconEnabled ~= nil then 
            UserInputService.MouseIconEnabled = prevMouseIconEnabled 
        end
    end
end

-- Botão fechar
closeBtn.MouseButton1Click:Connect(function() 
    showPanel(false)
end)

-- ═══════════════════════════════════════════════════════════════
--                      PÁGINA DO AIMBOT
-- ═══════════════════════════════════════════════════════════════

aimbotPage:FindFirstChildOfClass("UIListLayout"):Destroy()

local aimbotMainCard = Instance.new("Frame")
aimbotMainCard.Size = UDim2.new(1, 0, 0, 220)
aimbotMainCard.BackgroundColor3 = Color3.fromRGB(28,28,38)
aimbotMainCard.Parent = aimbotPage
Instance.new("UICorner", aimbotMainCard).CornerRadius = UDim.new(0, 12)
local aimbotStroke = Instance.new("UIStroke", aimbotMainCard)
aimbotStroke.Color = Color3.fromRGB(255, 70, 70)
aimbotStroke.Thickness = 2
aimbotStroke.Transparency = 0.7

local aimbotIcon = Instance.new("TextLabel")
aimbotIcon.Size = UDim2.new(0, 40, 0, 40)
aimbotIcon.Position = UDim2.new(0, 12, 0, 12)
aimbotIcon.BackgroundColor3 = Color3.fromRGB(255, 70, 70)
aimbotIcon.BackgroundTransparency = 0.8
aimbotIcon.Text = "🎯"
aimbotIcon.Font = Enum.Font.GothamBold
aimbotIcon.TextSize = 20
aimbotIcon.TextColor3 = Color3.fromRGB(255,255,255)
aimbotIcon.Parent = aimbotMainCard
Instance.new("UICorner", aimbotIcon).CornerRadius = UDim.new(0, 10)

local aimbotTitle = Instance.new("TextLabel")
aimbotTitle.Size = UDim2.new(1, -140, 0, 20)
aimbotTitle.Position = UDim2.new(0, 60, 0, 15)
aimbotTitle.BackgroundTransparency = 1
aimbotTitle.Font = Enum.Font.GothamBold
aimbotTitle.TextSize = 15
aimbotTitle.TextColor3 = Color3.fromRGB(255,255,255)
aimbotTitle.TextXAlignment = Enum.TextXAlignment.Left
aimbotTitle.Text = "AIMBOT"
aimbotTitle.Parent = aimbotMainCard

local aimbotSubtitle = Instance.new("TextLabel")
aimbotSubtitle.Size = UDim2.new(1, -140, 0, 16)
aimbotSubtitle.Position = UDim2.new(0, 60, 0, 35)
aimbotSubtitle.BackgroundTransparency = 1
aimbotSubtitle.Font = Enum.Font.Gotham
aimbotSubtitle.TextSize = 11
aimbotSubtitle.TextColor3 = Color3.fromRGB(150,150,160)
aimbotSubtitle.TextXAlignment = Enum.TextXAlignment.Left
aimbotSubtitle.Text = "Mira automática nos inimigos"
aimbotSubtitle.Parent = aimbotMainCard

local aimbotToggle = Instance.new("TextButton")
aimbotToggle.Parent = aimbotMainCard
aimbotToggle.Size = UDim2.new(0, 100, 0, 32)
aimbotToggle.Position = UDim2.new(1, -110, 0, 14)
aimbotToggle.Text = "OFF"
aimbotToggle.TextSize = 13
aimbotToggle.Font = Enum.Font.GothamBold
aimbotToggle.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
aimbotToggle.TextColor3 = Color3.fromRGB(255,255,255)
Instance.new("UICorner", aimbotToggle).CornerRadius = UDim.new(0, 10)
aimbotToggle.MouseButton1Click:Connect(function()
	aimbotEnabled = not aimbotEnabled
	aimbotToggle.Text = aimbotEnabled and "ON" or "OFF"
	aimbotToggle.BackgroundColor3 = aimbotEnabled and Color3.fromRGB(0, 160, 90) or Color3.fromRGB(60, 60, 70)
end)

local function createFOVSlider()
	local container = Instance.new("Frame")
	container.Size = UDim2.new(1, -24, 0, 60)
	container.Position = UDim2.new(0, 12, 0, 60)
	container.BackgroundTransparency = 1
	container.Parent = aimbotMainCard

	local label = Instance.new("TextLabel")
	label.Size = UDim2.new(0.5, -5, 0, 20)
	label.Position = UDim2.new(0, 0, 0, 0)
	label.BackgroundTransparency = 1
	label.Font = Enum.Font.GothamBold
	label.TextSize = 12
	label.TextColor3 = Color3.fromRGB(200,200,200)
	label.TextXAlignment = Enum.TextXAlignment.Left
	label.Text = "FOV"
	label.Parent = container

	local valueLabel = Instance.new("TextLabel")
	valueLabel.Size = UDim2.new(0, 50, 0, 20)
	valueLabel.Position = UDim2.new(1, -50, 0, 0)
	valueLabel.BackgroundColor3 = Color3.fromRGB(255, 70, 70)
	valueLabel.Font = Enum.Font.GothamBold
	valueLabel.TextSize = 12
	valueLabel.TextColor3 = Color3.fromRGB(255,255,255)
	valueLabel.Text = tostring(aimbotFOV)
	valueLabel.Parent = container
	Instance.new("UICorner", valueLabel).CornerRadius = UDim.new(0, 6)

	local sliderBg = Instance.new("Frame")
	sliderBg.Size = UDim2.new(1, 0, 0, 8)
	sliderBg.Position = UDim2.new(0, 0, 0, 30)
	sliderBg.BackgroundColor3 = Color3.fromRGB(40,40,50)
	sliderBg.Parent = container
	Instance.new("UICorner", sliderBg).CornerRadius = UDim.new(1, 0)

	local sliderFill = Instance.new("Frame")
	sliderFill.Size = UDim2.new((aimbotFOV - 20) / (150 - 20), 0, 1, 0)
	sliderFill.BackgroundColor3 = Color3.fromRGB(255, 70, 70)
	sliderFill.Parent = sliderBg
	Instance.new("UICorner", sliderFill).CornerRadius = UDim.new(1, 0)

	local sliderBtn = Instance.new("TextButton")
	sliderBtn.Size = UDim2.new(0, 20, 0, 20)
	sliderBtn.Position = UDim2.new((aimbotFOV - 20) / (150 - 20), -10, 0.5, -10)
	sliderBtn.BackgroundColor3 = Color3.fromRGB(255,255,255)
	sliderBtn.Text = ""
	sliderBtn.Parent = sliderBg
	Instance.new("UICorner", sliderBtn).CornerRadius = UDim.new(1, 0)

	local dragging = false
	sliderBtn.MouseButton1Down:Connect(function() dragging = true end)
	UserInputService.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end
	end)

	RunService.RenderStepped:Connect(function()
		if dragging then
			local mousePos = UserInputService:GetMouseLocation().X
			local sliderPos = sliderBg.AbsolutePosition.X
			local sliderSize = sliderBg.AbsoluteSize.X
			local relativePos = math.clamp((mousePos - sliderPos) / sliderSize, 0, 1)
			aimbotFOV = math.floor(20 + (150 - 20) * relativePos)
			sliderFill.Size = UDim2.new(relativePos, 0, 1, 0)
			sliderBtn.Position = UDim2.new(relativePos, -10, 0.5, -10)
			valueLabel.Text = tostring(aimbotFOV)
		end
	end)
end

createFOVSlider()

local targetToggle = Instance.new("TextButton")
targetToggle.Size = UDim2.new(0.48, -5, 0, 35)
targetToggle.Position = UDim2.new(0, 12, 0, 175)
targetToggle.Text = "🎯 CABEÇA"
targetToggle.Font = Enum.Font.GothamBold
targetToggle.TextSize = 13
targetToggle.BackgroundColor3 = Color3.fromRGB(255, 70, 70)
targetToggle.TextColor3 = Color3.fromRGB(255,255,255)
targetToggle.Parent = aimbotMainCard
Instance.new("UICorner", targetToggle).CornerRadius = UDim.new(0, 8)
targetToggle.MouseButton1Click:Connect(function()
	aimbotTarget = aimbotTarget == "Head" and "HumanoidRootPart" or "Head"
	targetToggle.Text = aimbotTarget == "Head" and "🎯 CABEÇA" or "🎯 CORPO"
end)

local function createSmoothSlider()
	local container = Instance.new("Frame")
	container.Size = UDim2.new(1, -24, 0, 60)
	container.Position = UDim2.new(0, 12, 0, 125)
	container.BackgroundTransparency = 1
	container.Parent = aimbotMainCard

	local label = Instance.new("TextLabel")
	label.Size = UDim2.new(0.5, -5, 0, 20)
	label.Position = UDim2.new(0, 0, 0, 0)
	label.BackgroundTransparency = 1
	label.Font = Enum.Font.GothamBold
	label.TextSize = 12
	label.TextColor3 = Color3.fromRGB(200,200,200)
	label.TextXAlignment = Enum.TextXAlignment.Left
	label.Text = "SUAVIDADE"
	label.Parent = container

	local valueLabel = Instance.new("TextLabel")
	valueLabel.Size = UDim2.new(0, 50, 0, 20)
	valueLabel.Position = UDim2.new(1, -50, 0, 0)
	valueLabel.BackgroundColor3 = Color3.fromRGB(255, 70, 70)
	valueLabel.Font = Enum.Font.GothamBold
	valueLabel.TextSize = 12
	valueLabel.TextColor3 = Color3.fromRGB(255,255,255)
	valueLabel.Text = tostring(aimbotSmooth)
	valueLabel.Parent = container
	Instance.new("UICorner", valueLabel).CornerRadius = UDim.new(0, 6)

	local sliderBg = Instance.new("Frame")
	sliderBg.Size = UDim2.new(1, 0, 0, 8)
	sliderBg.Position = UDim2.new(0, 0, 0, 30)
	sliderBg.BackgroundColor3 = Color3.fromRGB(40,40,50)
	sliderBg.Parent = container
	Instance.new("UICorner", sliderBg).CornerRadius = UDim.new(1, 0)

	local sliderFill = Instance.new("Frame")
	sliderFill.Size = UDim2.new((aimbotSmooth - 1) / (20 - 1), 0, 1, 0)
	sliderFill.BackgroundColor3 = Color3.fromRGB(255, 70, 70)
	sliderFill.Parent = sliderBg
	Instance.new("UICorner", sliderFill).CornerRadius = UDim.new(1, 0)

	local sliderBtn = Instance.new("TextButton")
	sliderBtn.Size = UDim2.new(0, 20, 0, 20)
	sliderBtn.Position = UDim2.new((aimbotSmooth - 1) / (20 - 1), -10, 0.5, -10)
	sliderBtn.BackgroundColor3 = Color3.fromRGB(255,255,255)
	sliderBtn.Text = ""
	sliderBtn.Parent = sliderBg
	Instance.new("UICorner", sliderBtn).CornerRadius = UDim.new(1, 0)

	local dragging = false
	sliderBtn.MouseButton1Down:Connect(function() dragging = true end)
	UserInputService.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end
	end)

	RunService.RenderStepped:Connect(function()
		if dragging then
			local mousePos = UserInputService:GetMouseLocation().X
			local sliderPos = sliderBg.AbsolutePosition.X
			local sliderSize = sliderBg.AbsoluteSize.X
			local relativePos = math.clamp((mousePos - sliderPos) / sliderSize, 0, 1)
			aimbotSmooth = math.floor(1 + (20 - 1) * relativePos)
			sliderFill.Size = UDim2.new(relativePos, 0, 1, 0)
			sliderBtn.Position = UDim2.new(relativePos, -10, 0.5, -10)
			valueLabel.Text = tostring(aimbotSmooth)
		end
	end)
end

createSmoothSlider()

local keyBindBtn = Instance.new("TextButton")
keyBindBtn.Size = UDim2.new(0.48, -5, 0, 35)
keyBindBtn.Position = UDim2.new(0.52, 0, 0, 175)
keyBindBtn.Text = "⏱️ " .. aimbotKeyName
keyBindBtn.Font = Enum.Font.GothamBold
keyBindBtn.TextSize = 13
keyBindBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
keyBindBtn.TextColor3 = Color3.fromRGB(255,255,255)
keyBindBtn.Parent = aimbotMainCard
Instance.new("UICorner", keyBindBtn).CornerRadius = UDim.new(0, 8)

keyBindBtn.MouseButton1Click:Connect(function()
	waitingForKey = true
	keyBindBtn.Text = "PRESSIONE..."
	keyBindBtn.BackgroundColor3 = Color3.fromRGB(139, 92, 246)
end)

local aimbotInfo = Instance.new("TextLabel")
aimbotInfo.Size = UDim2.new(1, 0, 0, 90)
aimbotInfo.Position = UDim2.new(0, 0, 0, 230)
aimbotInfo.BackgroundColor3 = Color3.fromRGB(28,28,38)
aimbotInfo.Font = Enum.Font.Gotham
aimbotInfo.TextSize = 12
aimbotInfo.TextColor3 = Color3.fromRGB(180,180,200)
aimbotInfo.TextWrapped = true
aimbotInfo.Text = "  COMO USAR:\n\n  • Ative o Aimbot e ajuste FOV/Suavidade\n  • Escolha o alvo (Cabeça/Corpo) e tecla\n  • Segure a tecla para mirar automaticamente"
aimbotInfo.Parent = aimbotPage
Instance.new("UICorner", aimbotInfo).CornerRadius = UDim.new(0, 12)

-- ═══════════════════════════════════════════════════════════════
--                      PÁGINA DO CLICKER
-- ═══════════════════════════════════════════════════════════════

clickerPage:FindFirstChildOfClass("UIListLayout"):Destroy()

local clickerMainCard = Instance.new("Frame")
clickerMainCard.Size = UDim2.new(1, 0, 0, 180)
clickerMainCard.BackgroundColor3 = Color3.fromRGB(28,28,38)
clickerMainCard.Parent = clickerPage
Instance.new("UICorner", clickerMainCard).CornerRadius = UDim.new(0, 12)
local clickerStroke = Instance.new("UIStroke", clickerMainCard)
clickerStroke.Color = Color3.fromRGB(0, 160, 220)
clickerStroke.Thickness = 2
clickerStroke.Transparency = 0.7

local clickerIcon = Instance.new("TextLabel")
clickerIcon.Size = UDim2.new(0, 40, 0, 40)
clickerIcon.Position = UDim2.new(0, 12, 0, 12)
clickerIcon.BackgroundColor3 = Color3.fromRGB(0, 160, 220)
clickerIcon.BackgroundTransparency = 0.8
clickerIcon.Text = "🖱️"
clickerIcon.Font = Enum.Font.GothamBold
clickerIcon.TextSize = 20
clickerIcon.TextColor3 = Color3.fromRGB(255,255,255)
clickerIcon.Parent = clickerMainCard
Instance.new("UICorner", clickerIcon).CornerRadius = UDim.new(0, 10)

local clickerTitle = Instance.new("TextLabel")
clickerTitle.Size = UDim2.new(1, -140, 0, 20)
clickerTitle.Position = UDim2.new(0, 60, 0, 15)
clickerTitle.BackgroundTransparency = 1
clickerTitle.Font = Enum.Font.GothamBold
clickerTitle.TextSize = 15
clickerTitle.TextColor3 = Color3.fromRGB(255,255,255)
clickerTitle.TextXAlignment = Enum.TextXAlignment.Left
clickerTitle.Text = "AUTO CLICKER"
clickerTitle.Parent = clickerMainCard

local clickerSubtitle = Instance.new("TextLabel")
clickerSubtitle.Size = UDim2.new(1, -140, 0, 16)
clickerSubtitle.Position = UDim2.new(0, 60, 0, 35)
clickerSubtitle.BackgroundTransparency = 1
clickerSubtitle.Font = Enum.Font.Gotham
clickerSubtitle.TextSize = 11
clickerSubtitle.TextColor3 = Color3.fromRGB(150,150,160)
clickerSubtitle.TextXAlignment = Enum.TextXAlignment.Left
clickerSubtitle.Text = "Cliques automáticos rápidos"
clickerSubtitle.Parent = clickerMainCard

local clickerToggle = Instance.new("TextButton")
clickerToggle.Parent = clickerMainCard
clickerToggle.Size = UDim2.new(0, 100, 0, 32)
clickerToggle.Position = UDim2.new(1, -110, 0, 14)
clickerToggle.Text = "OFF"
clickerToggle.TextSize = 13
clickerToggle.Font = Enum.Font.GothamBold
clickerToggle.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
clickerToggle.TextColor3 = Color3.fromRGB(255,255,255)
Instance.new("UICorner", clickerToggle).CornerRadius = UDim.new(0, 10)
clickerToggle.MouseButton1Click:Connect(function()
	autoClickerEnabled = not autoClickerEnabled
	clickerToggle.Text = autoClickerEnabled and "ON" or "OFF"
	clickerToggle.BackgroundColor3 = autoClickerEnabled and Color3.fromRGB(0, 160, 90) or Color3.fromRGB(60, 60, 70)
end)

local function createClickerSpeedSlider()
	local container = Instance.new("Frame")
	container.Size = UDim2.new(1, -24, 0, 60)
	container.Position = UDim2.new(0, 12, 0, 60)
	container.BackgroundTransparency = 1
	container.Parent = clickerMainCard

	local label = Instance.new("TextLabel")
	label.Size = UDim2.new(0.5, -5, 0, 20)
	label.Position = UDim2.new(0, 0, 0, 0)
	label.BackgroundTransparency = 1
	label.Font = Enum.Font.GothamBold
	label.TextSize = 12
	label.TextColor3 = Color3.fromRGB(200,200,200)
	label.TextXAlignment = Enum.TextXAlignment.Left
	label.Text = "VELOCIDADE (MS)"
	label.Parent = container

	local valueLabel = Instance.new("TextLabel")
	valueLabel.Size = UDim2.new(0, 50, 0, 20)
	valueLabel.Position = UDim2.new(1, -50, 0, 0)
	valueLabel.BackgroundColor3 = Color3.fromRGB(0, 160, 220)
	valueLabel.Font = Enum.Font.GothamBold
	valueLabel.TextSize = 12
	valueLabel.TextColor3 = Color3.fromRGB(255,255,255)
	valueLabel.Text = tostring(autoClickerSpeed)
	valueLabel.Parent = container
	Instance.new("UICorner", valueLabel).CornerRadius = UDim.new(0, 6)

	local sliderBg = Instance.new("Frame")
	sliderBg.Size = UDim2.new(1, 0, 0, 8)
	sliderBg.Position = UDim2.new(0, 0, 0, 30)
	sliderBg.BackgroundColor3 = Color3.fromRGB(40,40,50)
	sliderBg.Parent = container
	Instance.new("UICorner", sliderBg).CornerRadius = UDim.new(1, 0)

	local sliderFill = Instance.new("Frame")
	sliderFill.Size = UDim2.new((autoClickerSpeed - 1) / (100 - 1), 0, 1, 0)
	sliderFill.BackgroundColor3 = Color3.fromRGB(0, 160, 220)
	sliderFill.Parent = sliderBg
	Instance.new("UICorner", sliderFill).CornerRadius = UDim.new(1, 0)

	local sliderBtn = Instance.new("TextButton")
	sliderBtn.Size = UDim2.new(0, 20, 0, 20)
	sliderBtn.Position = UDim2.new((autoClickerSpeed - 1) / (100 - 1), -10, 0.5, -10)
	sliderBtn.BackgroundColor3 = Color3.fromRGB(255,255,255)
	sliderBtn.Text = ""
	sliderBtn.Parent = sliderBg
	Instance.new("UICorner", sliderBtn).CornerRadius = UDim.new(1, 0)

	local dragging = false
	sliderBtn.MouseButton1Down:Connect(function() dragging = true end)
	UserInputService.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end
	end)

	RunService.RenderStepped:Connect(function()
		if dragging then
			local mousePos = UserInputService:GetMouseLocation().X
			local sliderPos = sliderBg.AbsolutePosition.X
			local sliderSize = sliderBg.AbsoluteSize.X
			local relativePos = math.clamp((mousePos - sliderPos) / sliderSize, 0, 1)
			autoClickerSpeed = math.floor(1 + (100 - 1) * relativePos)
			sliderFill.Size = UDim2.new(relativePos, 0, 1, 0)
			sliderBtn.Position = UDim2.new(relativePos, -10, 0.5, -10)
			valueLabel.Text = tostring(autoClickerSpeed)
		end
	end)
end

createClickerSpeedSlider()

local clickerKeyBtn = Instance.new("TextButton")
clickerKeyBtn.Size = UDim2.new(1, -24, 0, 35)
clickerKeyBtn.Position = UDim2.new(0, 12, 0, 135)
clickerKeyBtn.Text = "⏱️ TECLA: " .. autoClickerKeyName
clickerKeyBtn.Font = Enum.Font.GothamBold
clickerKeyBtn.TextSize = 13
clickerKeyBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
clickerKeyBtn.TextColor3 = Color3.fromRGB(255,255,255)
clickerKeyBtn.Parent = clickerMainCard
Instance.new("UICorner", clickerKeyBtn).CornerRadius = UDim.new(0, 8)

clickerKeyBtn.MouseButton1Click:Connect(function()
	waitingForClickerKey = true
	clickerKeyBtn.Text = "PRESSIONE..."
	clickerKeyBtn.BackgroundColor3 = Color3.fromRGB(139, 92, 246)
end)

local clickerInfo = Instance.new("TextLabel")
clickerInfo.Size = UDim2.new(1, 0, 0, 90)
clickerInfo.Position = UDim2.new(0, 0, 0, 190)
clickerInfo.BackgroundColor3 = Color3.fromRGB(28,28,38)
clickerInfo.Font = Enum.Font.Gotham
clickerInfo.TextSize = 12
clickerInfo.TextColor3 = Color3.fromRGB(180,180,200)
clickerInfo.TextWrapped = true
clickerInfo.Text = "  COMO USAR:\n\n  • Ative o Auto Clicker e ajuste velocidade\n  • Escolha a tecla de ativação\n  • Segure a tecla para clicar automaticamente"
clickerInfo.Parent = clickerPage
Instance.new("UICorner", clickerInfo).CornerRadius = UDim.new(0, 12)

-- ═══════════════════════════════════════════════════════════════
--                      PÁGINA DE JOGADORES
-- ═══════════════════════════════════════════════════════════════

playersPage:FindFirstChildOfClass("UIListLayout"):Destroy()

local playersListFrame = Instance.new("Frame")
playersListFrame.Size = UDim2.new(0.48, 0, 1, 0)
playersListFrame.Position = UDim2.new(0, 0, 0, 0)
playersListFrame.BackgroundColor3 = Color3.fromRGB(28,28,38)
playersListFrame.Parent = playersPage
Instance.new("UICorner", playersListFrame).CornerRadius = UDim.new(0, 12)
local playersFrameStroke = Instance.new("UIStroke", playersListFrame)
playersFrameStroke.Color = Color3.fromRGB(45,45,55)
playersFrameStroke.Thickness = 2
playersFrameStroke.Transparency = 0.5

local playersListTitle = Instance.new("TextLabel")
playersListTitle.BackgroundTransparency = 1
playersListTitle.Size = UDim2.new(1, 0, 0, 40)
playersListTitle.Font = Enum.Font.GothamBold
playersListTitle.TextSize = 17
playersListTitle.TextColor3 = Color3.fromRGB(255,255,255)
playersListTitle.Text = "👥 JOGADORES ONLINE"
playersListTitle.Parent = playersListFrame

local searchBox = Instance.new("TextBox")
searchBox.Size = UDim2.new(1, -16, 0, 35)
searchBox.Position = UDim2.new(0, 8, 0, 45)
searchBox.BackgroundColor3 = Color3.fromRGB(15,15,22)
searchBox.PlaceholderText = "🔍 Pesquisar jogador..."
searchBox.PlaceholderColor3 = Color3.fromRGB(100,100,110)
searchBox.Text = ""
searchBox.Font = Enum.Font.Gotham
searchBox.TextSize = 14
searchBox.TextColor3 = Color3.fromRGB(255,255,255)
searchBox.TextXAlignment = Enum.TextXAlignment.Left
searchBox.ClearTextOnFocus = false
searchBox.Parent = playersListFrame
Instance.new("UICorner", searchBox).CornerRadius = UDim.new(0, 8)
local searchPadding = Instance.new("UIPadding", searchBox)
searchPadding.PaddingLeft = UDim.new(0, 10)

local playersList = Instance.new("ScrollingFrame")
playersList.Size = UDim2.new(1, -16, 1, -92)
playersList.Position = UDim2.new(0, 8, 0, 86)
playersList.CanvasSize = UDim2.new(0,0,0,0)
playersList.ScrollBarThickness = 6
playersList.BackgroundColor3 = Color3.fromRGB(15,15,22)
playersList.BorderSizePixel = 0
playersList.Parent = playersListFrame
Instance.new("UICorner", playersList).CornerRadius = UDim.new(0,10)
local playersLayout = Instance.new("UIListLayout", playersList)
playersLayout.Padding = UDim.new(0,6)

local actionsFrame = Instance.new("Frame")
actionsFrame.Size = UDim2.new(0.48, 0, 1, 0)
actionsFrame.Position = UDim2.new(0.52, 0, 0, 0)
actionsFrame.BackgroundColor3 = Color3.fromRGB(28,28,38)
actionsFrame.Parent = playersPage
Instance.new("UICorner", actionsFrame).CornerRadius = UDim.new(0, 12)
local actionsFrameStroke = Instance.new("UIStroke", actionsFrame)
actionsFrameStroke.Color = Color3.fromRGB(45,45,55)
actionsFrameStroke.Thickness = 2
actionsFrameStroke.Transparency = 0.5

local selectedPlayerTitle = Instance.new("TextLabel")
selectedPlayerTitle.BackgroundTransparency = 1
selectedPlayerTitle.Size = UDim2.new(1, 0, 0, 40)
selectedPlayerTitle.Font = Enum.Font.GothamBold
selectedPlayerTitle.TextSize = 17
selectedPlayerTitle.TextColor3 = Color3.fromRGB(255,255,255)
selectedPlayerTitle.Text = "SELECIONE UM JOGADOR"
selectedPlayerTitle.Parent = actionsFrame

local actionsScroll = Instance.new("ScrollingFrame")
actionsScroll.Size = UDim2.new(1, -24, 1, -52)
actionsScroll.Position = UDim2.new(0, 12, 0, 46)
actionsScroll.BackgroundTransparency = 1
actionsScroll.BorderSizePixel = 0
actionsScroll.ScrollBarThickness = 6
actionsScroll.CanvasSize = UDim2.new(0,0,0,0)
actionsScroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
actionsScroll.Parent = actionsFrame
local actionsLayout = Instance.new("UIListLayout", actionsScroll)
actionsLayout.Padding = UDim.new(0,10)
actionsLayout.SortOrder = Enum.SortOrder.LayoutOrder

local function createActionButton(text, color, callback)
	local btn = Instance.new("TextButton")
	btn.Size = UDim2.new(1, 0, 0, 50)
	btn.Text = text
	btn.Font = Enum.Font.GothamBold
	btn.TextSize = 16
	btn.BackgroundColor3 = color
	btn.TextColor3 = Color3.fromRGB(255,255,255)
	btn.Parent = actionsScroll
	Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 12)
	local stroke = Instance.new("UIStroke", btn)
	stroke.Color = Color3.fromRGB(0,0,0)
	stroke.Thickness = 1
	stroke.Transparency = 0.7
	btn.MouseButton1Click:Connect(function()
		if selectedPlayer then callback(selectedPlayer) end
	end)
	btn.MouseEnter:Connect(function()
		btn.BackgroundColor3 = Color3.new(color.R * 1.15, color.G * 1.15, color.B * 1.15)
	end)
	btn.MouseLeave:Connect(function()
		btn.BackgroundColor3 = color
	end)
	return btn
end

createActionButton("📍 TELEPORTAR PARA JOGADOR", Color3.fromRGB(0,160,90), instantTPTo)
createActionButton("👁️ ESPECTAR JOGADOR", Color3.fromRGB(160,120,0), startSpectate)
createActionButton("🔄 PARAR DE ESPECTAR", Color3.fromRGB(200,50,50), stopSpectate)
createActionButton("👤 VER PERFIL DO JOGADOR", Color3.fromRGB(100,100,200), function(p)
	game:GetService("GuiService"):InspectPlayerFromUserId(p.UserId)
end)
createActionButton("➕ ADICIONAR COMO AMIGO", Color3.fromRGB(50,150,50), function(p)
	LocalPlayer:RequestFriendship(p)
end)

local function createPlayerButton(player)
	if not player or player == LocalPlayer then return end
	local btn = Instance.new("TextButton")
	btn.Name = player.Name
	btn.Size = UDim2.new(1, -12, 0, 55)
	btn.BackgroundColor3 = Color3.fromRGB(30,30,42)
	btn.Text = ""
	btn.Parent = playersList
	Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 10)
	local btnStroke = Instance.new("UIStroke", btn)
	btnStroke.Color = Color3.fromRGB(50,50,60)
	btnStroke.Thickness = 1
	btnStroke.Transparency = 0.5

	local avatar = Instance.new("ImageLabel")
	avatar.Size = UDim2.new(0, 40, 0, 40)
	avatar.Position = UDim2.new(0, 8, 0, 7)
	avatar.BackgroundColor3 = Color3.fromRGB(50,50,60)
	avatar.Image = "https://www.roblox.com/headshot-thumbnail/image?userId="..player.UserId.."&width=48&height=48&format=png"
	avatar.Parent = btn
	Instance.new("UICorner", avatar).CornerRadius = UDim.new(1, 0)

	local nameLbl = Instance.new("TextLabel")
	nameLbl.BackgroundTransparency = 1
	nameLbl.Size = UDim2.new(1, -60, 0, 25)
	nameLbl.Position = UDim2.new(0, 55, 0, 8)
	nameLbl.Font = Enum.Font.GothamBold
	nameLbl.TextSize = 16
	nameLbl.TextColor3 = Color3.fromRGB(255,255,255)
	nameLbl.TextXAlignment = Enum.TextXAlignment.Left
	nameLbl.Text = player.Name
	nameLbl.Parent = btn

	local statusLbl = Instance.new("TextLabel")
	statusLbl.BackgroundTransparency = 1
	statusLbl.Size = UDim2.new(1, -60, 0, 18)
	statusLbl.Position = UDim2.new(0, 55, 0, 30)
	statusLbl.Font = Enum.Font.Gotham
	statusLbl.TextSize = 12
	statusLbl.TextColor3 = Color3.fromRGB(150,150,160)
	statusLbl.TextXAlignment = Enum.TextXAlignment.Left
	statusLbl.Text = "ID: "..player.UserId
	statusLbl.Parent = btn

	local indicator = Instance.new("Frame")
	indicator.Size = UDim2.new(0, 4, 1, -14)
	indicator.Position = UDim2.new(0, 2, 0, 7)
	indicator.BackgroundColor3 = Color3.fromRGB(139, 92, 246)
	indicator.Visible = false
	indicator.Parent = btn
	Instance.new("UICorner", indicator).CornerRadius = UDim.new(1, 0)

	btn.MouseButton1Click:Connect(function()
		for _, button in pairs(playerButtons) do
			if button:IsA("TextButton") and button:FindFirstChild("Frame") then
				button.Frame.Visible = false
				button.BackgroundColor3 = Color3.fromRGB(30,30,42)
			end
		end
		indicator.Visible = true
		btn.BackgroundColor3 = Color3.fromRGB(45,45,60)
		selectedPlayer = player
		selectedPlayerTitle.Text = "⚙️ AÇÕES: " .. player.Name
	end)

	playerButtons[player] = btn
end

-- ═══════════════════════════════════════════════════════════════
--                      PÁGINA DE MOVIMENTO
-- ═══════════════════════════════════════════════════════════════

movementPage:FindFirstChildOfClass("UIListLayout"):Destroy()

local topRow = Instance.new("Frame")
topRow.Size = UDim2.new(1, 0, 0, 200)
topRow.BackgroundTransparency = 1
topRow.Parent = movementPage

local speedCard = Instance.new("Frame")
speedCard.Size = UDim2.new(0.48, -5, 1, 0)
speedCard.Position = UDim2.new(0, 0, 0, 0)
speedCard.BackgroundColor3 = Color3.fromRGB(28,28,38)
speedCard.Parent = topRow
Instance.new("UICorner", speedCard).CornerRadius = UDim.new(0, 12)
local speedStroke = Instance.new("UIStroke", speedCard)
speedStroke.Color = Color3.fromRGB(120, 80, 220)
speedStroke.Thickness = 2
speedStroke.Transparency = 0.7

local speedIcon = Instance.new("TextLabel")
speedIcon.Size = UDim2.new(0, 40, 0, 40)
speedIcon.Position = UDim2.new(0, 12, 0, 12)
speedIcon.BackgroundColor3 = Color3.fromRGB(120, 80, 220)
speedIcon.BackgroundTransparency = 0.8
speedIcon.Text = "🏃"
speedIcon.Font = Enum.Font.GothamBold
speedIcon.TextSize = 22
speedIcon.TextColor3 = Color3.fromRGB(255,255,255)
speedIcon.Parent = speedCard
Instance.new("UICorner", speedIcon).CornerRadius = UDim.new(0, 10)

local speedTitle = Instance.new("TextLabel")
speedTitle.Size = UDim2.new(1, -140, 0, 20)
speedTitle.Position = UDim2.new(0, 60, 0, 15)
speedTitle.BackgroundTransparency = 1
speedTitle.Font = Enum.Font.GothamBold
speedTitle.TextSize = 15
speedTitle.TextColor3 = Color3.fromRGB(255,255,255)
speedTitle.TextXAlignment = Enum.TextXAlignment.Left
speedTitle.Text = "VELOCIDADE"
speedTitle.Parent = speedCard

local speedSubtitle = Instance.new("TextLabel")
speedSubtitle.Size = UDim2.new(1, -140, 0, 16)
speedSubtitle.Position = UDim2.new(0, 60, 0, 35)
speedSubtitle.BackgroundTransparency = 1
speedSubtitle.Font = Enum.Font.Gotham
speedSubtitle.TextSize = 11
speedSubtitle.TextColor3 = Color3.fromRGB(150,150,160)
speedSubtitle.TextXAlignment = Enum.TextXAlignment.Left
speedSubtitle.Text = "Controle sua velocidade"
speedSubtitle.Parent = speedCard

local speedBoostActive = false
local speedConnection = nil
local currentSpeed = 16

local speedToggle = Instance.new("TextButton")
speedToggle.Size = UDim2.new(0, 100, 0, 32)
speedToggle.Position = UDim2.new(1, -110, 0, 14)
speedToggle.Text = "ATIVAR"
speedToggle.TextSize = 13
speedToggle.Font = Enum.Font.GothamBold
speedToggle.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
speedToggle.TextColor3 = Color3.fromRGB(255,255,255)
speedToggle.Parent = speedCard
Instance.new("UICorner", speedToggle).CornerRadius = UDim.new(0, 10)
speedToggle.MouseButton1Click:Connect(function()
	speedBoostActive = not speedBoostActive
	speedToggle.Text = speedBoostActive and "DESATIVAR" or "ATIVAR"
	speedToggle.BackgroundColor3 = speedBoostActive and Color3.fromRGB(0, 160, 90) or Color3.fromRGB(200, 50, 50)

	if speedBoostActive then
		speedConnection = RunService.Heartbeat:Connect(function()
			local char = LocalPlayer.Character
			if char then
				local hum = char:FindFirstChildOfClass("Humanoid")
				if hum then
					hum.WalkSpeed = currentSpeed
				end

				if currentSpeed > 16 then
					local rootPart = char:FindFirstChild("HumanoidRootPart")
					if rootPart then
						rootPart.AssemblyLinearVelocity = Vector3.new(rootPart.AssemblyLinearVelocity.X, rootPart.AssemblyLinearVelocity.Y, rootPart.AssemblyLinearVelocity.Z)
					end
				end
			end
		end)
	else
		if speedConnection then
			speedConnection:Disconnect()
			speedConnection = nil
		end

		local char = LocalPlayer.Character
		if char then
			local hum = char:FindFirstChildOfClass("Humanoid")
			if hum then hum.WalkSpeed = 16 end
		end
	end
end)

speedToggle.MouseEnter:Connect(function()
	speedToggle.BackgroundColor3 = speedBoostActive and Color3.fromRGB(0, 180, 110) or Color3.fromRGB(220, 70, 70)
end)

speedToggle.MouseLeave:Connect(function()
	speedToggle.BackgroundColor3 = speedBoostActive and Color3.fromRGB(0, 160, 90) or Color3.fromRGB(200, 50, 50)
end)

local function createSliderSpeed(parent, labelText, yPos)
	local valueLabel = Instance.new("TextLabel")
	valueLabel.Size = UDim2.new(0, 60, 0, 25)
	valueLabel.Position = UDim2.new(0, 10, 0, yPos)
	valueLabel.BackgroundColor3 = Color3.fromRGB(120, 80, 220)
	valueLabel.Font = Enum.Font.GothamBold
	valueLabel.TextSize = 14
	valueLabel.TextColor3 = Color3.fromRGB(255,255,255)
	valueLabel.Text = "16"
	valueLabel.Parent = parent
	Instance.new("UICorner", valueLabel).CornerRadius = UDim.new(0, 6)

	local speeds = {16, 25, 35, 50, 75, 100, 150, 200, 300, 500}
	local currentIndex = 1

	local btnPrev = Instance.new("TextButton")
	btnPrev.Size = UDim2.new(0, 50, 0, 25)
	btnPrev.Position = UDim2.new(0, 80, 0, yPos)
	btnPrev.Text = "<"
	btnPrev.Font = Enum.Font.GothamBold
	btnPrev.TextSize = 16
	btnPrev.BackgroundColor3 = Color3.fromRGB(120, 80, 220)
	btnPrev.TextColor3 = Color3.fromRGB(255,255,255)
	btnPrev.Parent = parent
	Instance.new("UICorner", btnPrev).CornerRadius = UDim.new(0, 6)

	local btnNext = Instance.new("TextButton")
	btnNext.Size = UDim2.new(0, 50, 0, 25)
	btnNext.Position = UDim2.new(1, -60, 0, yPos)
	btnNext.Text = ">"
	btnNext.Font = Enum.Font.GothamBold
	btnNext.TextSize = 16
	btnNext.BackgroundColor3 = Color3.fromRGB(120, 80, 220)
	btnNext.TextColor3 = Color3.fromRGB(255,255,255)
	btnNext.Parent = parent
	Instance.new("UICorner", btnNext).CornerRadius = UDim.new(0, 6)

	local speedBar = Instance.new("Frame")
	speedBar.Size = UDim2.new(1, -200, 0, 8)
	speedBar.Position = UDim2.new(0, 140, 0, yPos + 8)
	speedBar.BackgroundColor3 = Color3.fromRGB(40,40,50)
	speedBar.Parent = parent
	Instance.new("UICorner", speedBar).CornerRadius = UDim.new(1, 0)

	local speedFill = Instance.new("Frame")
	speedFill.Size = UDim2.new(0.1, 0, 1, 0)
	speedFill.BackgroundColor3 = Color3.fromRGB(120, 80, 220)
	speedFill.Parent = speedBar
	Instance.new("UICorner", speedFill).CornerRadius = UDim.new(1, 0)

	btnPrev.MouseButton1Click:Connect(function()
		if currentIndex > 1 then
			currentIndex = currentIndex - 1
			currentSpeed = speeds[currentIndex]
			valueLabel.Text = tostring(currentSpeed)
			speedFill.Size = UDim2.new(currentIndex / #speeds, 0, 1, 0)
			if speedBoostActive then
				local char = LocalPlayer.Character
				if char then
					local hum = char:FindFirstChildOfClass("Humanoid")
					if hum then hum.WalkSpeed = currentSpeed end
				end
			end
		end
	end)

	btnNext.MouseButton1Click:Connect(function()
		if currentIndex < #speeds then
			currentIndex = currentIndex + 1
			currentSpeed = speeds[currentIndex]
			valueLabel.Text = tostring(currentSpeed)
			speedFill.Size = UDim2.new(currentIndex / #speeds, 0, 1, 0)
			if speedBoostActive then
				local char = LocalPlayer.Character
				if char then
					local hum = char:FindFirstChildOfClass("Humanoid")
					if hum then hum.WalkSpeed = currentSpeed end
				end
			end
		end
	end)
end

createSliderSpeed(speedCard, "", 100)

local speedHint = Instance.new("TextLabel")
speedHint.Size = UDim2.new(1, -24, 0, 40)
speedHint.Position = UDim2.new(0, 12, 0, 140)
speedHint.BackgroundColor3 = Color3.fromRGB(120, 80, 220)
speedHint.BackgroundTransparency = 0.92
speedHint.Font = Enum.Font.Gotham
speedHint.TextSize = 11
speedHint.TextColor3 = Color3.fromRGB(180,180,200)
speedHint.TextWrapped = true
speedHint.Text = "Use < > para ajustar\n16 - 500 velocidade"
speedHint.Parent = speedCard
Instance.new("UICorner", speedHint).CornerRadius = UDim.new(0, 8)

local flyCard = Instance.new("Frame")
flyCard.Size = UDim2.new(0.48, -5, 1, 0)
flyCard.Position = UDim2.new(0.52, 0, 0, 0)
flyCard.BackgroundColor3 = Color3.fromRGB(28,28,38)
flyCard.Parent = topRow
Instance.new("UICorner", flyCard).CornerRadius = UDim.new(0, 12)
local flyStroke = Instance.new("UIStroke", flyCard)
flyStroke.Color = Color3.fromRGB(0, 160, 220)
flyStroke.Thickness = 2
flyStroke.Transparency = 0.7

local flyIcon = Instance.new("TextLabel")
flyIcon.Size = UDim2.new(0, 40, 0, 40)
flyIcon.Position = UDim2.new(0, 12, 0, 12)
flyIcon.BackgroundColor3 = Color3.fromRGB(0, 160, 220)
flyIcon.BackgroundTransparency = 0.8
flyIcon.Text = "✈️"
flyIcon.Font = Enum.Font.GothamBold
flyIcon.TextSize = 20
flyIcon.TextColor3 = Color3.fromRGB(255,255,255)
flyIcon.Parent = flyCard
Instance.new("UICorner", flyIcon).CornerRadius = UDim.new(0, 10)

local flyTitle = Instance.new("TextLabel")
flyTitle.Size = UDim2.new(1, -70, 0, 20)
flyTitle.Position = UDim2.new(0, 60, 0, 15)
flyTitle.BackgroundTransparency = 1
flyTitle.Font = Enum.Font.GothamBold
flyTitle.TextSize = 15
flyTitle.TextColor3 = Color3.fromRGB(255,255,255)
flyTitle.TextXAlignment = Enum.TextXAlignment.Left
flyTitle.Text = "SISTEMA DE VOO"
flyTitle.Parent = flyCard

local flySubtitle = Instance.new("TextLabel")
flySubtitle.Size = UDim2.new(1, -70, 0, 16)
flySubtitle.Position = UDim2.new(0, 60, 0, 35)
flySubtitle.BackgroundTransparency = 1
flySubtitle.Font = Enum.Font.Gotham
flySubtitle.TextSize = 11
flySubtitle.TextColor3 = Color3.fromRGB(150,150,160)
flySubtitle.TextXAlignment = Enum.TextXAlignment.Left
flySubtitle.Text = "Voe livremente pelo mapa"
flySubtitle.Parent = flyCard

local function createSlider(parent, labelText, minVal, maxVal, defaultVal, yPos, callback)
	local valueLabel = Instance.new("TextLabel")
	valueLabel.Size = UDim2.new(0, 60, 0, 25)
	valueLabel.Position = UDim2.new(0, 10, 0, yPos)
	valueLabel.BackgroundColor3 = Color3.fromRGB(0, 160, 220)
	valueLabel.Font = Enum.Font.GothamBold
	valueLabel.TextSize = 14
	valueLabel.TextColor3 = Color3.fromRGB(255,255,255)
	valueLabel.Text = tostring(defaultVal)
	valueLabel.Parent = parent
	Instance.new("UICorner", valueLabel).CornerRadius = UDim.new(0, 6)

	local btnPrev = Instance.new("TextButton")
	btnPrev.Size = UDim2.new(0, 50, 0, 25)
	btnPrev.Position = UDim2.new(0, 80, 0, yPos)
	btnPrev.Text = "<"
	btnPrev.Font = Enum.Font.GothamBold
	btnPrev.TextSize = 16
	btnPrev.BackgroundColor3 = Color3.fromRGB(0, 160, 220)
	btnPrev.TextColor3 = Color3.fromRGB(255,255,255)
	btnPrev.Parent = parent
	Instance.new("UICorner", btnPrev).CornerRadius = UDim.new(0, 6)

	local btnNext = Instance.new("TextButton")
	btnNext.Size = UDim2.new(0, 50, 0, 25)
	btnNext.Position = UDim2.new(1, -60, 0, yPos)
	btnNext.Text = ">"
	btnNext.Font = Enum.Font.GothamBold
	btnNext.TextSize = 16
	btnNext.BackgroundColor3 = Color3.fromRGB(0, 160, 220)
	btnNext.TextColor3 = Color3.fromRGB(255,255,255)
	btnNext.Parent = parent
	Instance.new("UICorner", btnNext).CornerRadius = UDim.new(0, 6)

	local speedBar = Instance.new("Frame")
	speedBar.Size = UDim2.new(1, -200, 0, 8)
	speedBar.Position = UDim2.new(0, 140, 0, yPos + 8)
	speedBar.BackgroundColor3 = Color3.fromRGB(40,40,50)
	speedBar.Parent = parent
	Instance.new("UICorner", speedBar).CornerRadius = UDim.new(1, 0)

	local speedFill = Instance.new("Frame")
	speedFill.Size = UDim2.new((defaultVal - minVal) / (maxVal - minVal), 0, 1, 0)
	speedFill.BackgroundColor3 = Color3.fromRGB(0, 160, 220)
	speedFill.Parent = speedBar
	Instance.new("UICorner", speedFill).CornerRadius = UDim.new(1, 0)

	btnPrev.MouseButton1Click:Connect(function()
		local currentVal = tonumber(valueLabel.Text) or defaultVal
		if currentVal > minVal then
			local newVal = math.max(minVal, currentVal - math.ceil((maxVal - minVal) / 10))
			valueLabel.Text = tostring(newVal)
			speedFill.Size = UDim2.new((newVal - minVal) / (maxVal - minVal), 0, 1, 0)
			callback(newVal)
		end
	end)

	btnNext.MouseButton1Click:Connect(function()
		local currentVal = tonumber(valueLabel.Text) or defaultVal
		if currentVal < maxVal then
			local newVal = math.min(maxVal, currentVal + math.ceil((maxVal - minVal) / 10))
			valueLabel.Text = tostring(newVal)
			speedFill.Size = UDim2.new((newVal - minVal) / (maxVal - minVal), 0, 1, 0)
			callback(newVal)
		end
	end)
end

createSlider(flyCard, "Velocidade", 10, 100, 50, 100, function(value)
	_G.FlySystem.speed = value
end)

local flyToggle = Instance.new("TextButton")
flyToggle.Size = UDim2.new(0.48, -5, 0, 35)
flyToggle.Position = UDim2.new(0, 12, 0, 145)
flyToggle.Text = "🚀 FLY: OFF"
flyToggle.Font = Enum.Font.GothamBold
flyToggle.TextSize = 13
flyToggle.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
flyToggle.TextColor3 = Color3.fromRGB(255,255,255)
flyToggle.Parent = flyCard
Instance.new("UICorner", flyToggle).CornerRadius = UDim.new(0, 10)
flyToggle.MouseButton1Click:Connect(function()
	_G.FlySystem:toggleFly()
	flyToggle.Text = _G.FlySystem.flying and "FLY: ON" or "FLY: OFF"
	flyToggle.BackgroundColor3 = _G.FlySystem.flying and Color3.fromRGB(0, 160, 90) or Color3.fromRGB(60, 60, 70)
end)

flyToggle.MouseEnter:Connect(function()
	flyToggle.BackgroundColor3 = _G.FlySystem.flying and Color3.fromRGB(0, 180, 110) or Color3.fromRGB(80, 80, 90)
end)

flyToggle.MouseLeave:Connect(function()
	flyToggle.BackgroundColor3 = _G.FlySystem.flying and Color3.fromRGB(0, 160, 90) or Color3.fromRGB(60, 60, 70)
end)

local noclipToggle = Instance.new("TextButton")
noclipToggle.Size = UDim2.new(0.48, -5, 0, 35)
noclipToggle.Position = UDim2.new(0.52, 0, 0, 145)
noclipToggle.Text = "🌫️ NOCLIP: OFF"
noclipToggle.Font = Enum.Font.GothamBold
noclipToggle.TextSize = 13
noclipToggle.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
noclipToggle.TextColor3 = Color3.fromRGB(255,255,255)
noclipToggle.Parent = flyCard
Instance.new("UICorner", noclipToggle).CornerRadius = UDim.new(0, 10)
noclipToggle.MouseButton1Click:Connect(function()
	_G.FlySystem:toggleNoclip()
	noclipToggle.Text = _G.FlySystem.noclip and "NOCLIP: ON" or "NOCLIP: OFF"
	noclipToggle.BackgroundColor3 = _G.FlySystem.noclip and Color3.fromRGB(0, 160, 90) or Color3.fromRGB(60, 60, 70)
end)

noclipToggle.MouseEnter:Connect(function()
	noclipToggle.BackgroundColor3 = _G.FlySystem.noclip and Color3.fromRGB(0, 180, 110) or Color3.fromRGB(80, 80, 90)
end)

noclipToggle.MouseLeave:Connect(function()
	noclipToggle.BackgroundColor3 = _G.FlySystem.noclip and Color3.fromRGB(0, 160, 90) or Color3.fromRGB(60, 60, 70)
end)

local infoCard = Instance.new("Frame")
infoCard.Size = UDim2.new(1, 0, 0, 110)
infoCard.Position = UDim2.new(0, 0, 0, 210)
infoCard.BackgroundColor3 = Color3.fromRGB(28,28,38)
infoCard.Parent = movementPage
Instance.new("UICorner", infoCard).CornerRadius = UDim.new(0, 12)
local infoStroke = Instance.new("UIStroke", infoCard)
infoStroke.Color = Color3.fromRGB(139, 92, 246)
infoStroke.Thickness = 2
infoStroke.Transparency = 0.7

local infoIconLabel = Instance.new("TextLabel")
infoIconLabel.Size = UDim2.new(0, 40, 0, 40)
infoIconLabel.Position = UDim2.new(0, 12, 0, 12)
infoIconLabel.BackgroundColor3 = Color3.fromRGB(139, 92, 246)
infoIconLabel.BackgroundTransparency = 0.8
infoIconLabel.Text = "ℹ️"
infoIconLabel.Font = Enum.Font.GothamBold
infoIconLabel.TextSize = 20
infoIconLabel.TextColor3 = Color3.fromRGB(255,255,255)
infoIconLabel.Parent = infoCard
Instance.new("UICorner", infoIconLabel).CornerRadius = UDim.new(0, 10)

local infoTitleLabel = Instance.new("TextLabel")
infoTitleLabel.Size = UDim2.new(1, -70, 0, 20)
infoTitleLabel.Position = UDim2.new(0, 60, 0, 15)
infoTitleLabel.BackgroundTransparency = 1
infoTitleLabel.Font = Enum.Font.GothamBold
infoTitleLabel.TextSize = 15
infoTitleLabel.TextColor3 = Color3.fromRGB(255,255,255)
infoTitleLabel.TextXAlignment = Enum.TextXAlignment.Left
infoTitleLabel.Text = "CONTROLES"
infoTitleLabel.Parent = infoCard

local infoText = Instance.new("TextLabel")
infoText.Size = UDim2.new(1, -24, 0, 60)
infoText.Position = UDim2.new(0, 12, 0, 45)
infoText.BackgroundTransparency = 1
infoText.Font = Enum.Font.Gotham
infoText.TextSize = 12
infoText.TextColor3 = Color3.fromRGB(180,180,200)
infoText.TextWrapped = true
infoText.TextXAlignment = Enum.TextXAlignment.Left
infoText.TextYAlignment = Enum.TextYAlignment.Top
infoText.Text = "• F: Ativar/Desativar Fly\n• WASD: Movimento | Space: Subir | Ctrl: Descer\n• Noclip permite atravessar paredes"
infoText.Parent = infoCard

-- ═══════════════════════════════════════════════════════════════
--                      PÁGINA DE VISÃO
-- ═══════════════════════════════════════════════════════════════

visionPage:FindFirstChildOfClass("UIListLayout"):Destroy()

local visionTopRow = Instance.new("Frame")
visionTopRow.Size = UDim2.new(1, 0, 0, 140)
visionTopRow.BackgroundTransparency = 1
visionTopRow.Parent = visionPage

local espCard = Instance.new("Frame")
espCard.Size = UDim2.new(0.48, -5, 1, 0)
espCard.Position = UDim2.new(0, 0, 0, 0)
espCard.BackgroundColor3 = Color3.fromRGB(28,28,38)
espCard.Parent = visionTopRow
Instance.new("UICorner", espCard).CornerRadius = UDim.new(0, 12)
local espStroke = Instance.new("UIStroke", espCard)
espStroke.Color = Color3.fromRGB(0, 255, 150)
espStroke.Thickness = 2
espStroke.Transparency = 0.7

local espIcon = Instance.new("TextLabel")
espIcon.Size = UDim2.new(0, 35, 0, 35)
espIcon.Position = UDim2.new(0, 12, 0, 12)
espIcon.BackgroundColor3 = Color3.fromRGB(0, 255, 150)
espIcon.BackgroundTransparency = 0.8
espIcon.Text = "👁️"
espIcon.Font = Enum.Font.GothamBold
espIcon.TextSize = 18
espIcon.TextColor3 = Color3.fromRGB(255,255,255)
espIcon.Parent = espCard
Instance.new("UICorner", espIcon).CornerRadius = UDim.new(0, 8)

local espTitle = Instance.new("TextLabel")
espTitle.Size = UDim2.new(1, -130, 0, 18)
espTitle.Position = UDim2.new(0, 55, 0, 15)
espTitle.BackgroundTransparency = 1
espTitle.Font = Enum.Font.GothamBold
espTitle.TextSize = 14
espTitle.TextColor3 = Color3.fromRGB(255,255,255)
espTitle.TextXAlignment = Enum.TextXAlignment.Left
espTitle.Text = "ESP WALLHACK"
espTitle.Parent = espCard

local espSubtitle = Instance.new("TextLabel")
espSubtitle.Size = UDim2.new(1, -130, 0, 14)
espSubtitle.Position = UDim2.new(0, 55, 0, 32)
espSubtitle.BackgroundTransparency = 1
espSubtitle.Font = Enum.Font.Gotham
espSubtitle.TextSize = 10
espSubtitle.TextColor3 = Color3.fromRGB(150,150,160)
espSubtitle.TextXAlignment = Enum.TextXAlignment.Left
espSubtitle.Text = "Ver jogadores através de paredes"
espSubtitle.Parent = espCard

local espToggle = Instance.new("TextButton")
espToggle.Size = UDim2.new(0, 80, 0, 28)
espToggle.Position = UDim2.new(1, -90, 0, 12)
espToggle.Text = "OFF"
espToggle.Font = Enum.Font.GothamBold
espToggle.TextSize = 12
espToggle.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
espToggle.TextColor3 = Color3.fromRGB(255,255,255)
espToggle.Parent = espCard
Instance.new("UICorner", espToggle).CornerRadius = UDim.new(0, 8)
espToggle.MouseButton1Click:Connect(function()
    espEnabled = not espEnabled
    espToggle.Text = espEnabled and "ON" or "OFF"
    espToggle.BackgroundColor3 = espEnabled and Color3.fromRGB(0, 160, 90) or Color3.fromRGB(60, 60, 70)
end)

local espDistanceSlider = Instance.new("Frame")
espDistanceSlider.Size = UDim2.new(1, -24, 0, 32)
espDistanceSlider.Position = UDim2.new(0, 12, 0, 60)
espDistanceSlider.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
espDistanceSlider.Parent = espCard
Instance.new("UICorner", espDistanceSlider).CornerRadius = UDim.new(0, 8)

local distanceLabel = Instance.new("TextLabel")
distanceLabel.Size = UDim2.new(0.5, 0, 1, 0)
distanceLabel.BackgroundTransparency = 1
distanceLabel.Font = Enum.Font.GothamBold
distanceLabel.TextSize = 12
distanceLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
distanceLabel.Text = "DISTÂNCIA: " .. espMaxDistance
distanceLabel.Parent = espDistanceSlider

local distanceDown = Instance.new("TextButton")
distanceDown.Size = UDim2.new(0, 30, 0, 24)
distanceDown.Position = UDim2.new(1, -70, 0, 4)
distanceDown.Text = "-"
distanceDown.Font = Enum.Font.GothamBold
distanceDown.TextSize = 16
distanceDown.BackgroundColor3 = Color3.fromRGB(255, 70, 70)
distanceDown.TextColor3 = Color3.fromRGB(255, 255, 255)
distanceDown.Parent = espDistanceSlider
Instance.new("UICorner", distanceDown).CornerRadius = UDim.new(0, 4)

local distanceUp = Instance.new("TextButton")
distanceUp.Size = UDim2.new(0, 30, 0, 24)
distanceUp.Position = UDim2.new(1, -35, 0, 4)
distanceUp.Text = "+"
distanceUp.Font = Enum.Font.GothamBold
distanceUp.TextSize = 16
distanceUp.BackgroundColor3 = Color3.fromRGB(0, 160, 90)
distanceUp.TextColor3 = Color3.fromRGB(255, 255, 255)
distanceUp.Parent = espDistanceSlider
Instance.new("UICorner", distanceUp).CornerRadius = UDim.new(0, 4)

distanceDown.MouseButton1Click:Connect(function()
    espMaxDistance = math.max(100, espMaxDistance - 100)
    distanceLabel.Text = "DISTÂNCIA: " .. espMaxDistance
end)

distanceUp.MouseButton1Click:Connect(function()
    espMaxDistance = math.min(1000, espMaxDistance + 100)
    distanceLabel.Text = "DISTÂNCIA: " .. espMaxDistance
end)

local espBoxBtn = Instance.new("TextButton")
espBoxBtn.Size = UDim2.new(0.48, -5, 0, 32)
espBoxBtn.Position = UDim2.new(0, 12, 0, 100)
espBoxBtn.Text = "□ BOXES"
espBoxBtn.Font = Enum.Font.GothamBold
espBoxBtn.TextSize = 11
espBoxBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
espBoxBtn.TextColor3 = Color3.fromRGB(255,255,255)
espBoxBtn.Parent = espCard
Instance.new("UICorner", espBoxBtn).CornerRadius = UDim.new(0, 8)
espBoxBtn.MouseButton1Click:Connect(function()
    espBoxEnabled = not espBoxEnabled
    espBoxBtn.Text = espBoxEnabled and "■ ON" or "□ BOXES"
    espBoxBtn.BackgroundColor3 = espBoxEnabled and Color3.fromRGB(0, 160, 90) or Color3.fromRGB(60, 60, 70)
end)

local espHealthBtn = Instance.new("TextButton")
espHealthBtn.Size = UDim2.new(0.48, -5, 0, 32)
espHealthBtn.Position = UDim2.new(0.52, 0, 0, 100)
espHealthBtn.Text = "❤️ HEALTH"
espHealthBtn.Font = Enum.Font.GothamBold
espHealthBtn.TextSize = 11
espHealthBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
espHealthBtn.TextColor3 = Color3.fromRGB(255,255,255)
espHealthBtn.Parent = espCard
Instance.new("UICorner", espHealthBtn).CornerRadius = UDim.new(0, 8)
local espHealthEnabled = false
espHealthBtn.MouseButton1Click:Connect(function()
	espHealthEnabled = not espHealthEnabled
	espHealthBtn.Text = espHealthEnabled and "❤️ ON" or "❤️ HEALTH"
	espHealthBtn.BackgroundColor3 = espHealthEnabled and Color3.fromRGB(0, 160, 90) or Color3.fromRGB(60, 60, 70)
end)

local visualCard = Instance.new("Frame")
visualCard.Size = UDim2.new(0.48, -5, 1, 0)
visualCard.Position = UDim2.new(0.52, 0, 0, 0)
visualCard.BackgroundColor3 = Color3.fromRGB(28,28,38)
visualCard.Parent = visionTopRow
Instance.new("UICorner", visualCard).CornerRadius = UDim.new(0, 12)
local visualStroke = Instance.new("UIStroke", visualCard)
visualStroke.Color = Color3.fromRGB(255, 200, 0)
visualStroke.Thickness = 2
visualStroke.Transparency = 0.7

local visualIcon = Instance.new("TextLabel")
visualIcon.Size = UDim2.new(0, 35, 0, 35)
visualIcon.Position = UDim2.new(0, 12, 0, 12)
visualIcon.BackgroundColor3 = Color3.fromRGB(255, 200, 0)
visualIcon.BackgroundTransparency = 0.8
visualIcon.Text = "🌈"
visualIcon.Font = Enum.Font.GothamBold
visualIcon.TextSize = 18
visualIcon.TextColor3 = Color3.fromRGB(255,255,255)
visualIcon.Parent = visualCard
Instance.new("UICorner", visualIcon).CornerRadius = UDim.new(0, 8)

local visualTitle = Instance.new("TextLabel")
visualTitle.Size = UDim2.new(1, -60, 0, 18)
visualTitle.Position = UDim2.new(0, 55, 0, 15)
visualTitle.BackgroundTransparency = 1
visualTitle.Font = Enum.Font.GothamBold
visualTitle.TextSize = 14
visualTitle.TextColor3 = Color3.fromRGB(255,255,255)
visualTitle.TextXAlignment = Enum.TextXAlignment.Left
visualTitle.Text = "VISUAIS"
visualTitle.Parent = visualCard

local visualSubtitle = Instance.new("TextLabel")
visualSubtitle.Size = UDim2.new(1, -60, 0, 14)
visualSubtitle.Position = UDim2.new(0, 55, 0, 32)
visualSubtitle.BackgroundTransparency = 1
visualSubtitle.Font = Enum.Font.Gotham
visualSubtitle.TextSize = 10
visualSubtitle.TextColor3 = Color3.fromRGB(150,150,160)
visualSubtitle.TextXAlignment = Enum.TextXAlignment.Left
visualSubtitle.Text = "Melhorias visuais do jogo"
visualSubtitle.Parent = visualCard

local fullbrightBtn = Instance.new("TextButton")
fullbrightBtn.Size = UDim2.new(1, -24, 0, 32)
fullbrightBtn.Position = UDim2.new(0, 12, 0, 60)
fullbrightBtn.Text = "💡 FULLBRIGHT"
fullbrightBtn.Font = Enum.Font.GothamBold
fullbrightBtn.TextSize = 12
fullbrightBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
fullbrightBtn.TextColor3 = Color3.fromRGB(255,255,255)
fullbrightBtn.Parent = visualCard
Instance.new("UICorner", fullbrightBtn).CornerRadius = UDim.new(0, 8)
local fullbrightEnabled = false
fullbrightBtn.MouseButton1Click:Connect(function()
	fullbrightEnabled = not fullbrightEnabled
	fullbrightBtn.Text = fullbrightEnabled and "💡 ON" or "💡 FULLBRIGHT"
	fullbrightBtn.BackgroundColor3 = fullbrightEnabled and Color3.fromRGB(0, 160, 90) or Color3.fromRGB(60, 60, 70)
	game:GetService("Lighting").Brightness = fullbrightEnabled and 2 or 1
	game:GetService("Lighting").ClockTime = fullbrightEnabled and 14 or 12
	game:GetService("Lighting").FogEnd = fullbrightEnabled and 100000 or 500
end)

local removeBlurBtn = Instance.new("TextButton")
removeBlurBtn.Size.Size = UDim2.new(0.48, -5 = UDim2.new(0.48, -5, 0, 32)
removeBlurBtn.Position =, 0, 32 UDim2.new(0)
removeBlurBtn.Position = UDim2.new(0, 12, 0, 100)
removeBlurBtn, 12, 0, 100)
removeBlurBtn.Text = "🌫️ BLUR"
removeBlurBtn.Font = Enum.Text = "🌫️ BLUR"
removeBlurBtn.Font.Font.GothamBold = Enum.Font.GothamBold
removeBlurBtn.Text
removeBlurBtn.TextSize =Size = 11
removeBlurBtn.BackgroundColor3 = 11
removeBlurBtn.BackgroundColor3 = Color3.fromRGB(100 Color3.fromRGB(100, 50, 150)
remove, 50, 150)
removeBlurBtn.TextColor3BlurBtn.TextColor3 = Color3.fromRGB(255, = Color3.fromRGB(255,255,255)
removeBlurBtn255,255)
removeBlurBtn.Parent = visualCard
Instance.new.Parent = visualCard
("UICorner", removeInstance.new("UICorner", removeBlurBtn).CornerBlurBtn).CornerRadius =Radius = UDim.new(0, 8)
removeBlur UDim.new(0, 8)
removeBlurBtn.MouseButton1ClickBtn.MouseButton1Click:Connect:Connect(function()
(function()
	for _, obj in pairs(game:GetService("	for _, obj in pairs(game:GetService("Lighting"):GetChildren()) do
		if obj:IsLighting"):GetChildren()) do
		if obj:IsA("BlurA("BlurEffect") or objEffect") or obj:IsA(":IsA("BloomEffectBloomEffect") or obj:IsA("ColorCorrectionEffect")") or obj:IsA("ColorCorrectionEffect") then
 then
			obj:			obj:Destroy()
		endDestroy()
		end
	end
end)

local visionBottomRow =
	end
end)

local Instance.new("Frame")
visionBottomRow.Size = UDim visionBottomRow = Instance.new("Frame")
visionBottomRow.Size =2.new(1, 0, 0,  UDim2.new(1, 100)
visionBottomRow.BackgroundTrans0, 0, 100)
visionBottomRow.BackgroundTransparency = 1
visionBottomRow.Position = UDim2.new(0,parency = 1
visionBottomRow.Position = UDim2 0, 0, 150.new(0, 0, 0, 150)
visionBottomRow.Parent = vision)
visionBottomRow.Parent = visionPage

local tracersCard = Instance.new("Frame")
Page

local tracersCard = Instance.new("Frame")
tractracersCard.Size = UDim2.new(0.48, -5,ersCard.Size = UDim2.new(0.48, - 1, 0)
tracersCard.BackgroundColor3 = Color5, 1, 0)
tracersCard.BackgroundColor3 = Color3.from3.fromRGB(28,28,38)
tracersCard.ParentRGB(28,28,38)
tracersCard.Parent = visionBottomRow
Instance.new(" = visionBottomRow
Instance.new("UICorner", tracersCard).CornerRadius = UDim.new(UICorner", tracersCard).CornerRadius = UDim0, 12)
local tracersStroke = Instance.new.new(0, 12)
local tracersStroke = Instance.new("UIStroke", tracersCard)
tracersStroke.Color = Color3.fromRGB(139, 92, 246("UIStroke", tracersCard)
tracersStroke.Color = Color3.fromRGB(139)
tracersStroke.Thickness = 2
, 92, 246)
tracersStroke.Thickness = 2
tracersStroke.Transparency = 0tracersStroke.Transparency = 0.7

local tracersIcon = Instance.new("TextLabel")
trac.7

local tracersIcon = Instance.new("TextLabel")
tracersIcon.Size =ersIcon.Size = UDim2.new(0, 30, 0,  UDim2.new30)
tracersIcon.Position = U(0, 30, 0, 30)
tracersIcon.PositionDim2.new(0, 12 = UDim2.new(, 0, 12)
tracersIcon.Back0, 12, 0, 12)
tracersgroundColor3 = Color3.fromRGB(139, Icon.BackgroundColor3 = Color3.fromRGB(139, 92, 246)
tracers92, 246)
tracersIcon.BackgroundTransparency =Icon.BackgroundTransparency = 0.8
t 0.8
tracersIcon.Text = "📏racersIcon.Text = "📏"
tracersIcon.Font = Enum.Font.GothamBold"
tracersIcon.Font = Enum.Font.GothamBold
tracersIcon.TextSize = 16
tracersIcon.Text
tracersIcon.TextSize = 16
tracersIcon.TextColor3 = ColorColor3 = Color3.fromRGB(255,255,255)
tracersIcon3.fromRGB(255,255,255)
tracersIcon.Parent = tracersCard
Instance.Parent = tracersCard
Instance.new(".new("UICorner", tracersIcon).CornerRadius =UICorner", tracersIcon).CornerRadius = UDim.new(0, 8)

local tracersTitle = UDim.new(0, 8)

local tracers Instance.new("TextLabel")
tracersTitle.Size =Title = Instance.new("TextLabel")
tracersTitle.Size = UDim2.new UDim2.new(1, -120, 0, 16)
tracersTitle.Position = UDim2(1, -120, 0, 16)
tracersTitle.Position = UDim2.new(0, 50, 0,.new(0, 50, 0, 14 14)
tracersTitle.BackgroundTransparency = 1
tracersTitle.Font)
tracersTitle.BackgroundTransparency = 1
tracersTitle.Font = Enum.Font.GothamBold
t = Enum.Font.GothamBold
tracersTitle.TextSize = 13
tracersracersTitle.TextSize = 13
tracersTitle.TextTitle.TextColor3 = Color3.fromRGB(255,255Color3 = Color3.fromRGB(255,255,255)
tracersTitle.TextXAlignment = Enum.TextXAlignment,255)
tracersTitle.TextXAlignment = Enum.TextXAlignment.Left
tracers.Left
tracersTitle.TextTitle.Text = "TRAC = "TRACERS"
tracersTitle.Parent = tracersCard

local tracersERS"
tracersTitle.Parent = tracersCard

localBtn = Instance.new("TextButton")
tracersBtn.Size = UDim2.new(0 tracersBtn = Instance.new("TextButton")
tracersBtn.Size = UDim2.new(0, 70, 0, 26)
tracersBtn.Position = U, 70, 0, 26)
tracersBtn.Position = UDim2.new(1, -80Dim2.new(1, -80, 0, 12)
tracersBtn.Text, 0, 12)
tracersBtn.Text = " = "OFF"
tracersBtn.Font = Enum.Font.GothOFF"
tracersBtn.Font =amBold
tracersBtn.TextSize Enum.Font.GothamBold
tracersBtn.TextSize = 11
trac = 11
tracersBtn.BackgroundColor3 = Color3.fromRGB(60, 60, ersBtn.BackgroundColor3 = Color3.fromRGB(60,70)
tracersBtn.TextColor3 = Color3 60, 70)
tracersBtn.TextColor3 =.fromRGB(255,255,255)
tracersBtn.Parent = tracersCard
Instance.new("UIC Color3.fromRGB(255,255,255)
tracersBtn.Parent = tracersorner", tracersBtn).CornerRadius = UDim.new(0Card
Instance.new("UICorner", tracersBtn).CornerRadius = UDim.new(0, 8)
trac, 8)
tracersBtn.MouseButton1Click:Connect(function()
    tracersEnabled = not traersBtn.MouseButton1Click:Connect(function()
    tracerscersEnabled
    tracersBtn.Text = tracersEnabled and "Enabled = not tracersEnabled
    tracersBtn.Text = tracersEnabled and "ON" or "OFF"
    tracersBtnON" or "OFF"
    tracersBtn.BackgroundColor3 = tracersEnabled and Color3.fromRGB(.BackgroundColor3 = tracersEnabled and Color3.fromRGB(0, 160, 90)0, 160, 90) or Color3.fromRGB( or Color3.fromRGB(60, 60, 70)
end)

local nametags60, 60, 70)
end)

local nametagsCard = Instance.new("Frame")
nametagsCard = Instance.new("FrameCard.Size = UDim2.new(0.")
nametagsCard.Size = UDim2.new(0.48, -5, 1, 048, -5, 1, 0)
nametags)
nametagsCard.Position = UDim2.new(0.52Card.Position = UDim2.new(0.52, 0, 0, 0)
, 0, 0, 0)
nametagsCard.BackgroundColor3 = Color3.fromnametagsCard.BackgroundColor3 = Color3.fromRGB(28,28,38)
RGB(28,28,38)
nametagsCard.Parent = visionnametagsCard.Parent = visionBottomRow
Instance.new("BottomRow
Instance.new("UICorner", nametagsCard).CornerRadius = UUICorner", nametagsCard).CornerRadius = UDim.new(0, Dim.new(0, 12)
local nametags12)
local nametagsStroke = Instance.new("UIStroke",Stroke = Instance.new("UIStroke", nametagsCard)
nametagsStroke.Color = Color3.fromRGB(255, 100 nametagsCard)
nametagsStroke.Color = Color3.fromRGB(255, 100, 150)
nametagsStroke.Thickness = 2
nam, 150)
nametagsStroke.Thickness = 2
nametagsStroke.TransparencyetagsStroke.Transparency = 0.7

local nametagsIcon = Instance.new("TextLabel")
namet = 0.7

local nametagsIcon = Instance.newagsIcon.Size = UDim2.new(0, 30,("TextLabel")
nametagsIcon.Size = UDim2.new(0, 30, 0, 30)
nametagsIcon.Position = UDim2.new(0, 12, 0, 12)
nam 0, 30)
nametagsIcon.Position = UDim2.new(0, 12etagsIcon.BackgroundColor3 = Color3.fromRGB, 0, 12)
nametagsIcon.BackgroundColor3 = Color3.fromRGB(255, 100, 150)
nametagsIcon.Back(255, 100, 150)
nametagsIcon.BackgroundTransparency = 0groundTransparency = 0.8
nametagsIcon.Text = "🏷️"
nametagsIcon.Font = Enum.Font.Goth.8
nametagsIcon.Text = "🏷️"
nametagsIcon.Font = Enum.FontamBold
nametagsIcon.TextSize = 16
.GothamBold
nametagsIcon.TextSize = 16
nametagsIconnametagsIcon.TextColor3 = Color3.fromRGB(255,255,255)
nametagsIcon.Parent =.TextColor3 = Color3.fromRGB(255,255,255)
nametagsIcon.Parent = nametagsCard
Instance.new("UICorner", namet nametagsCard
Instance.new("UICorner", nametagsIcon).CornerRadiusagsIcon).CornerRadius = UDim.new(0 = UDim.new(0, 8)

local nametags, 8)

local nametagsTitle = Instance.new("TextLabel")
nametagsTitle.Size = UDim2.new(1, -Title = Instance.new("TextLabel")
nametagsTitle.Size = UDim2.new(1, -120, 0, 16)
nametagsTitle.Position = U120, 0, 16)
nametagsTitle.Position = UDim2.new(Dim2.new(0, 50, 0, 14)
nametagsTitle.Back0, 50, 0, 14)
nametagsTitle.BackgroundTransgroundTransparency = 1
nametagsTitle.Font = Enum.Font.GothamBoldparency = 1
nametagsTitle.Font = Enum.Font.GothamBold
nametags
nametagsTitle.TextSize = 13
nametagsTitle.TextColor3 = Color3.fromRGB(255,Title.TextSize = 13
nametagsTitle.TextColor3 = Color255,255)
nametagsTitle.TextXAlignment = Enum.TextXAlignment.Left
nametags3.fromRGB(255,255,255)
nametagsTitle.TextXAlignment = Enum.TextXAlignment.Left
nametagsTitle.Text = "NAMETAGS"
nametagsTitle.Parent = namTitle.Text = "NAMETAGS"
nametagsTitle.ParentetagsCard

local nametagsBtn = Instance.new("Text = nametagsCard

local nametagsBtn = Instance.new("TextButton")
nametagsBtn.Size = UDim2.newButton")
nametagsBtn.Size = UDim2.new(0, 70, 0, 26)
nametagsBtn.Position = U(0, 70, 0, 26)
nametDim2.new(1, -80, 0, 12agsBtn.Position = UDim2.new(1, -80, 0, 12)
nametagsBtn.Text = ")
nametagsBtn.Text = "OFF"
nametagsBtnOFF"
nametagsBtn.Font = Enum.Font.GothamB.Font = Enum.Font.GothamBold
nametagsBtn.TextSize = 11
nametagsBtn.BackgroundColor3old
nametagsBtn.TextSize = 11
namet = Color3.fromRGB(60, 60, 70)
agsBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
nametagsBtn.TextColor3 = Color3.fromRGB(255nametagsBtn.TextColor3 = Color3.fromRGB(255,255,255)
nametagsBtn.Parent = nametagsCard,255,255)
nametagsBtn.Parent = nametagsCard
Instance.new("UICor
Instance.new("UICorner", nametagsBtn).Corner", nametagsBtn).CornerRadius = UDim.newnerRadius = UDim.new(0, 8)
nametagsBtn.MouseButton1Click:Connect(function()
	namet(0, 8)
nametagsBtn.MouseButton1Click:Connect(function()
	agsEnabled = not nametagsEnabled
	nametagsBtnnametagsEnabled = not nametagsEnabled
	nametagsBtn.Text = nametagsEnabled.Text = nametagsEnabled and "ON" or "OFF"
	nametagsBtn.BackgroundColor3 = and "ON" or "OFF"
	nametagsBtn.Back nametagsEnabled and Color3.fromRGB(0, 160groundColor3 = nametagsEnabled and Color3.fromRGB(0, 160, 90) or Color3.fromRGB(, 90) or Color3.fromRGB(60, 6060, 60, 70)
end)

local vision, 70)
end)

local visionInfo = Instance.new("TextLabel")
visionInfo.Size = UDim2.new(1Info = Instance.new("TextLabel")
visionInfo.Size = UDim, 0, 0, 70)
visionInfo.Position = U2.new(1, 0, 0, 70)
visionInfo.Position = UDim2.new(0, 0Dim2.new(0, 0, 0, 260)
visionInfo.BackgroundColor3 =, 0, 260)
visionInfo.BackgroundColor3 = Color3.fromRGB(28,28,38)
visionInfo.Font = Enum.Font.Gotham
vision Color3.fromRGB(28,28,38)
visionInfo.Font = Enum.Font.Gotham
visionInfo.TextSize = 11Info.TextSize = 11
visionInfo.TextColor3 = Color
visionInfo.TextColor3 = Color3.fromRGB(180,180,200)
visionInfo3.fromRGB(180,180,200)
visionInfo.TextWrapped = true
visionInfo.Text = "  💡 DIC.TextWrapped = true
visionInfo.Text = "  💡 DICAS:\n  • ESPAS:\n  • ESP mostra jogadores através mostra jogadores através de paredes | • Fullbright ilumina tudo\n  • T de paredes | • Fullbright ilumina tudo\n  • Tracers desenha linracers desenha linhas | • Nametags mostra informaçõeshas | • Nametags mostra informações"
visionInfo.Parent ="
visionInfo.Parent = visionPage
Instance.new("UICor visionPage
Instance.new("UICorner", visionInfo).Corner", visionInfo).CornerRadius = UDim.new(0, 12)

--nerRadius = UDim.new(0, 12)

-- ═════════════════════════ ═══════════════════════════════════════════════════════════════════════
--══════════════════════════════
--                      PÁGINA MISC
--                      PÁGINA MISC
 ═════════════════════════════════-- ═══════════════════════════════════════════════════════════════

m══════════════════════════════

miscPage:FindFirstChildOfClass("UIListLayoutiscPage:FindFirstChildOfClass("UIListLayout"):"):Destroy()

local miscTopRow = Instance.new("Frame")
Destroy()

local miscTopRowmiscTopRow.Size = UDim2.new(1 = Instance.new("Frame")
miscTopRow.Size = UDim, 0, 0, 180)
misc2.new(1, 0, 0, 180)
miscTopRow.BackgroundTransparencyTopRow.BackgroundTransparency = 1
miscTopRow.Parent = miscPage

 = 1
miscTopRow.Parent = miscPagelocal characterCard = Instance.new("Frame")
characterCard.Size = UDim2.new(0.

local characterCard = Instance.new("Frame")
characterCard.Size = UDim2.new(0.48, -5, 48, -5, 1, 0)
characterCard.Back1, 0)
characterCard.BackgroundColor3 = Color3.fromRGBgroundColor3 = Color3.fromRGB(28,28,38)
characterCard.Parent = miscTopRow
Instance.new(28,28,38)
characterCard.Parent = miscTopRow
Instance.new("UICorner", characterCard).Corner("UICorner", characterCard).CornerRadius = UDimRadius = UDim.new(0, 12)
local charStroke = Instance.new("UIStroke.new(0, 12)
local charStroke = Instance.new("UIStroke", characterCard)
charStroke.Color = Color3", characterCard)
charStroke.Color = Color3.fromRGB(255, 100, 100)
char.fromRGB(255, 100, 100)
charStroke.Stroke.Thickness = 2
charStroke.Transparency =Thickness = 2
charStroke.Transparency = 0.7

local charIcon = Instance 0.7

local charIcon = Instance.new("TextLabel.new("TextLabel")
charIcon.Size = UDim2.new(0, 35, ")
charIcon.Size = UDim2.new(0, 35, 0, 35)
charIcon.Position = UDim0, 35)
charIcon.Position = UDim2.new(0, 12, 02.new(0, 12, 0, 12)
, 12)
charIcon.BackgroundColor3 = ColorcharIcon.BackgroundColor3 = Color3.fromRGB(255,3.fromRGB(255, 100,  100, 100)
charIcon.BackgroundTransparency = 100)
charIcon.BackgroundTransparency = 0.0.8
charIcon.Text = "🛡️"
8
charIcon.Text = "🛡️"
charIcon.Font = Enum.Font.GothamBold
charIcon.Font = Enum.Font.GothamBold
charIcon.TextSize = charIcon.TextSize = 18
charIcon.TextColor3 = Color318
charIcon.TextColor3 = Color3.fromRGB(255,255,255)
charIcon.Parent.fromRGB(255,255,255)
charIcon.Parent = characterCard
Instance.new(" = characterCard
Instance.new("UICorner", charIconUICorner", charIcon).CornerRadius = U).CornerRadius = UDim.new(0, 8)

Dim.new(0, 8)

local charTitle = Instance.new("TextLabel")
charTitle.Size = UDimlocal charTitle = Instance.new("TextLabel")
charTitle2.new(1, -60, 0, .Size = UDim2.new(1, -60, 0, 18)
charTitle.Position = UDim2.new(0, 55, 0, 1518)
charTitle.Position = UDim2.new(0,)
charTitle.BackgroundTransparency = 1
charTitle.Font 55, 0, 15)
charTitle.BackgroundTransparency = 1
charTitle.Font = Enum.Font.GothamBold
charTitle.Text = Enum.Font.GothamBold
charTitle.TextSize = 14
charTitle.TextColor3 = Color3.fromSize = 14
charTitle.TextColor3 = Color3.fromRGB(255,255,255)
RGB(255,255,255)
charTitle.TextXAlignment = Enum.TextXAlignment.LeftcharTitle.TextXAlignment = Enum.TextXAlignment.Left
char
charTitle.Text = "PERSONTitle.Text = "PERSONAGEM"
charTitle.ParentAGEM"
charTitle.Parent = characterCard

local charSubtitle = = characterCard

local charSub Instance.new("TextLabel")
charSubtitle.Size = UDim2.new(title = Instance.new("TextLabel")
charSubtitle.Size = UDim2.new(1, -60, 0, 141, -60, 0, 14)
charSubtitle.Position = UDim2.new(0,)
charSubtitle.Position = UDim2.new(0, 55 55, 0, 32)
charSubtitle, 0, 32)
charSubtitle.BackgroundTransparency.BackgroundTransparency = 1
charSubtitle.Font = Enum.Font.G = 1
charSubtitle.Font = Enum.Font.Gotham
charotham
charSubtitle.TextSize = 10
Subtitle.TextSize = 10
charSubtitle.TextColor3 = Color3.fromRGB(charSubtitle.TextColor3 = Color3.fromRGB(150,150,160)
charSubtitle.Text150,150,160)
charSubtitle.TextXAlignment = Enum.TextXAlignment.Left
charSubtitle.Text = "Modificações do personagem"
charSubtitle.ParentXAlignment = Enum.TextXAlignment.Left
charSubtitle.Text = "Modificações do personagem"
charSubtitle = characterCard

local infJumpBtn = Instance.new(".Parent = characterCard

local infJumpBtn = Instance.new("TextButton")
infTextButton")
infJumpBtn.Size = UDim2.new(1JumpBtn.Size = UDim2.new(1, -24, 0, 38)
infJumpBtn.Position = UDim2.new(0, -24, 0, 38)
infJumpBtn.Position = UDim2.new(0, 12, 0, 60)
infJumpBtn.Text = ", 12, 0, 60)
infJumpBtn.Text = "🦘 INF🦘 INF JUMP"
infJumpBtn.Font = Enum.Font.Goth JUMP"
infJumpBtn.Font = Enum.Font.GothamBold
infJumpBtn.TextamBold
infJumpBtn.TextSize = 13
infJumpBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
Size = 13
infJumpBtn.BackgroundColor3 = Color3.fromRGB(60, 60infJumpBtn.TextColor3 = Color3.fromRGB(255,, 70)
infJumpBtn.TextColor3 = Color3.fromRGB(255,255,255)
infJumpBtn.Parent =255,255)
infJumpBtn.Parent = characterCard
Instance.new(" characterCard
Instance.new("UICorner", infJumpBtn).CornerRadius = UDimUICorner", infJumpBtn).CornerRadius = UDim.new(0, 8)
local infJumpEnabled = false
.new(0, 8)
local infJumpEnabled = false
infJumpBtn.MouseButton1Click:Connect(function()
    infinfJumpBtn.MouseButton1Click:Connect(function()
    infJumpEnabled = not infJumpJumpEnabled = not infJumpEnabled
    infJumpBtn.Text = infJumpEnabled and "Enabled
    infJumpBtn.Text = infJumpEnabled and "🦘🦘 ON" or "🦘 INF JUMP"
    infJumpBtn.BackgroundColor3 = infJumpEnabled and Color3 ON" or "🦘 INF JUMP"
    infJumpBtn.BackgroundColor3 = infJumpEnabled and Color3.fromRGB(0, .fromRGB(0, 160, 90) or Color3160, 90) or Color3.fromRGB(60.fromRGB(60, 60, 70)
end)
User, 60, 70)
end)
UserInputService.JumpRequest:Connect(function()
    if infJumpEnabledInputService.JumpRequest:Connect(function()
    if infJumpEnabled and LocalPlayer.Character then
        local hum = LocalPlayer.Character:FindFirst and LocalPlayer.Character then
        local hum = LocalPlayer.CharacterChildOfClass("Humanoid")
        if hum then hum:ChangeState(Enum.HumanoidStateType.Jumping) end:FindFirstChildOfClass("Humanoid")
        if hum then hum:ChangeState(Enum.HumanoidStateType.Jumping
    end
end)

local bhopBtn = Instance.new("TextButton")
bhopBtn.Size =) end
    end
end)

local bhopBtn = Instance.new("TextButton")
b UDim2.new(1, -24, 0, hopBtn.Size = UDim2.new(1, -24, 0, 38)
bhopBtn.Position = UDim38)
bhopBtn.Position = UDim2.new(0, 12, 0, 108)
bhop2.new(0, 12, 0, 108)
Btn.Text = "🐇 BHOP"
bhopBtn.Font = Enum.Font.GothamBbhopBtn.Text = "🐇 BHOP"
bhopBtn.Font = Enum.Font.GothamBold
bhopBtn.TextSize = 13
bhopBtn.BackgroundColor3 = Color3.fromRGB(old
bhopBtn.TextSize = 13
bhopBtn.BackgroundColor3 = Color60, 60, 70)
bhopBtn.TextColor3 = Color3.fromRGB(60, 60, 70)
bhopBtn.TextColor3 = Color3.fromRGB(255,255,255)
bhopBtn.P3.fromRGB(255,255,255)
bhopBtn.Parent = characterCardarent = characterCard
Instance.new("UICorner", bhop
Instance.new("UICorner", bhopBtn).CornerRadius = UDim.new(0, 8)
local bhopEnabled = false
Btn).CornerRadius = UDim.new(0, 8)
local bhopEnabled = false
bhopBtn.MouseButton1Click:Connect(function()
    bhopEnabledbhopBtn.MouseButton1Click:Connect(function()
    bhopEnabled = not bhop = not bhopEnabled
    bhopBtn.Text = bhopEnabled and "Enabled
    bhopBtn.Text = bhopEnabled and "🐇 ON" or "🐇 BH🐇 ON" or "🐇 BHOP"
    bhopBtn.BackgroundColor3 = bhopEnabled and Color3.fromRGB(0, 160, OP"
    bhopBtn.BackgroundColor3 = bhopEnabled and Color3.fromRGB(90) or Color3.fromRGB(60, 60, 0, 160, 90) or Color3.fromRGB(60, 60, 70)
end)
RunService.Heart70)
end)
RunService.Heartbeat:Connect(functionbeat:Connect(function()
    if bhopEnabled and LocalPlayer.()
    if bhopEnabled and LocalPlayer.Character then
        local hum = LocalPlayer.Character then
        local humCharacter:FindFirstChildOfClass("Humanoid = LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if hum and hum.MoveDirection.Magnitude > ")
        if hum and hum.MoveDirection.Magnitude > 0 then
            hum:ChangeState(Enum.Human0 then
            hum:ChangeState(Enum.HumanoidStateoidStateType.Jumping)
        end
    end
end)

local utilityCard = Instance.new("FrameType.Jumping)
        end
    end
end)

local utility")
utilityCard.Size = UDim2.new(0.48, -5, Card = Instance.new("Frame")
utilityCard.Size = UDim2.new(0.48, -5, 1, 0)
utilityCard.Position = UDim2.new(0.521, 0)
utilityCard.Position = UDim2.new(0.52, 0, 0, 0)
utilityCard.BackgroundColor3 = Color3.from, 0, 0, 0)
utilityCard.BackgroundColor3 = Color3.fromRGB(28,28,38)
utilityCard.Parent = miscRGB(28,28,38)
utilityCard.Parent = miscTopRow
Instance.new("UICorTopRow
Instance.new("UICorner", utilityCard).CornerRadius = Uner", utilityCard).CornerRadius = UDim.new(0, 12)
local utilDim.new(0, 12)
local utilStroke = Instance.new("UIStroke = Instance.new("UIStroke", utilityCard)
utilStroke.Color = Color3.fromRGB(100,Stroke", utilityCard)
utilStroke.Color = Color3.fromRGB(100, 200, 255)
utilStroke.Thickness = 2
utilStroke.Transparency =  200, 255)
utilStroke.Thickness = 2
utilStroke.Transparency0.7

local utilIcon = Instance.new("TextLabel")
utilIcon.Size = = 0.7

local utilIcon = Instance.new("TextLabel")
utilIcon.Size = UDim2.new(0, 35, 0, 35)
utilIcon UDim2.new(0, 35, 0, 35)
utilIcon.Position = UDim2.new(0, 12, 0, 12)
util.Position = UDim2.new(0, 12, 0, 12)
utilIcon.BackgroundColor3 = Color3.fromRGB(100Icon.BackgroundColor3 = Color3.fromRGB(100, 200, 255)
utilIcon.BackgroundTransparency, 200, 255)
utilIcon.BackgroundTransparency = 0.8
util = 0.8
utilIcon.Text = "⚙️"
utilIcon.Font = Enum.FontIcon.Text = "⚙️"
utilIcon.Font = Enum.Font.Goth.GothamBold
utilIcon.TextSize = 18
amBold
utilIcon.TextSize = 18
utilIcon.TextColor3 = Color3.fromRGB(255,255,255)
utilIcon.Parent =utilIcon.TextColor3 = Color3.fromRGB(255,255,255)
utilIcon.P utilityCard
Instance.new("UICorner", utilIcon).CornerRadius = Uarent = utilityCard
Instance.new("UICorner", utilIcon).CornerRadius = UDim.new(0, 8)

local utilTitle = Instance.newDim.new(0, 8)

local utilTitle = Instance.new("TextLabel")
("TextLabel")
utilTitle.Size = UDim2.new(1, -60, 0, utilTitle.Size = UDim2.new(1, -60, 0, 18)
utilTitle.Position = UDim2.new(0, 55, 0,18)
utilTitle.Position = UDim2.new(0, 55, 0, 15)
utilTitle.BackgroundTrans 15)
utilTitle.BackgroundTransparency = 1
utilTitle.Font = Enumparency = 1
utilTitle.Font = Enum.Font.GothamBold
utilTitle.Text.Font.GothamBold
utilTitle.TextSize = 14
utilTitle.TextSize = 14
utilTitle.TextColor3 = Color3.fromRGB(255,255,255)
Color3 = Color3.fromRGB(255,255,255)
utilTitle.TextXAlignment = Enum.TextXAlignment.Left
utilTitle.Text = "UTILutilTitle.TextXAlignment = Enum.TextXAlignment.Left
utilTitle.Text = "UTILIDADES"
utilTitle.Parent = utilityCard

local utilSubtitle =IDADES"
utilTitle.Parent = utilityCard

local utilSubtitle = Instance.new("TextLabel")
utilSubtitle.Size = U Instance.new("TextLabel")
utilSubtitle.Size = UDim2.new(1, -60, 0, 14)
utilDim2.new(1, -60, 0, Subtitle.Position = UDim2.new(0, 55, 0,14)
utilSubtitle.Position = UDim2.new(0 32)
utilSubtitle.BackgroundTransparency, 55, 0, 32)
util = 1
utilSubSubtitle.BackgroundTransparencytitle.Font = Enum.Font.Gotham
utilSubtitle = 1
utilSubtitle.Font = Enum.Font.Gotham
utilSubtitle.TextSize = 10
utilSubtitle.Text.TextSize = 10
utilSubtitle.TextColor3 = Color3.fromRGB(150,150,160)
utilSubtitle.TextXAlignmentColor3 = Color3.fromRGB(150,150,160)
utilSubtitle.TextXAlignment = Enum.TextXAlignment.Left
utilSubtitle.Text = "Ferramentas = Enum.TextXAlignment.Left
utilSubtitle.Text = "Ferramentas úteis"
utilSubtitle.Parent = úteis"
utilSubtitle.Parent = utilityCard

local antiAfkBtn utilityCard

local antiAfkBtn = Instance.new("TextButton")
antiAfkBtn.Size = Instance.new("TextButton")
antiAfkBtn.Size = UDim2.new(1, -24, 0, 38)
antiAfkBtn.Position = UDim = UDim2.new(1, -24, 0, 38)
antiAfkBtn.P2.new(0, 12, 0, 60)
antiAfkBtn.Text =osition = UDim2.new(0, 12, 0, 60)
antiAfkBtn.Text = "⏰ ANTI-AFK"
antiAfkBtn.Font = Enum "⏰ ANTI-AFK"
antiAfkBtn.Font = Enum.Font.GothamBold
antiAfkBtn.TextSize = 13
antiAfkBtn.BackgroundColor3 = Color3.Font.GothamBold
antiAfkBtn.TextSize = 13
antiAfkBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
anti.fromRGB(60, 60, 70)
antiAfkBtn.TextColor3 = Color3.fromRGB(255,255,AfkBtn.TextColor3 = Color3.fromRGB(255,255,255)
antiAf255)
antiAfkBtn.Parent = utilityCard
Instance.new("UICorner", antiAfkkBtn.Parent = utilityCard
Instance.new("UICorner", antiAfkBtn).CornerRadius = UDim.new(0, 8)
local antiAfkEnabledBtn).CornerRadius = UDim.new(0, 8)
local antiAfkEnabled = false
local antiAfkConnection
antiAfkBtn.M = false
local antiAfkConnection
antiAfkBtn.MouseButton1Click:Connect(function()
	antiAfkEnabled =ouseButton1Click:Connect(function()
	antiAfkEnabled = not antiAfkEnabled
 not antiAfkEnabled
	antiAfkBtn.Text = antiAfkEnabled and "⏰	antiAfkBtn.Text = antiAfkEnabled and "⏰ ON" or " ON" or "⏰ ANTI-AFK"
	antiAfkBtn.BackgroundColor⏰ ANTI-AFK"
	antiAfkBtn.BackgroundColor3 = antiAfkEnabled and Color3.fromRGB(0, 1603 = antiAfkEnabled and Color3.fromRGB(0, 160, 90) or Color3.fromRGB(60,, 90) or Color3.fromRGB(60, 60 60, 70)
	if antiAfkEnabled then
	, 70)
	if antiAfkEnabled then
		game:GetService("	game:GetService("Players").LocalPlayer.Idled:Connect(function()
			game:GetService("VirtualUser"Players").LocalPlayer.Idled:Connect(function()
			game:GetService("VirtualUser"):Button2Down(Vector2.new):Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
			wait(1(0,0), workspace.CurrentCamera.CFrame)
			wait)
			game:GetService("VirtualUser"):Button2(1)
			game:GetService("VirtualUser"):Button2Up(Vector2.new(Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
		end)
	end
end)

local chatSpamBtn = Instance.new("TextButton0,0), workspace.CurrentCamera.CFrame)
		end)
	end
end)

local chatSpamBtn = Instance.new("TextButton")
chatSpamBtn.Size")
chatSpamBtn.Size = UDim2.new(1, = UDim2.new(1, -24, 0, 38)
chatSpam -24, 0, 38)
chatSpamBtn.Position = UDim2.new(0, Btn.Position = UDim2.new(0, 12, 0, 108)
chatSpamBtn.Text = "💬 SPAM"
chatSpamBtn12, 0, 108)
chatSpamBtn.Text = "💬 SPAM"
chatSpamBtn.Font = Enum.Font.GothamBold
chatSpamBtn.TextSize = 13
.Font = Enum.Font.GothamBold
chatSpamBtn.TextSize = 13
chatSpamBtn.BackgroundColor3 = Color3.fromchatSpamBtn.BackgroundColor3 = Color3.fromRGB(60,RGB(60, 60, 70)
chatSpamBtn.TextColor3 = Color3 60, 70)
chatSpamBtn.TextColor3 = Color3.fromRGB(255,255,255)
chatSpamBtn.Parent = utilityCard
Instance.new(".fromRGB(255,255,255)
chatSpamBtn.Parent = utilityCard
Instance.new("UICorner", chatSpamBtn).CornerRadius = UDim.newUICorner", chatSpamBtn).CornerRadius = UDim.new(0, 8)
local chat(0, 8)
local chatSpamEnabled = false
local chatSpamSpamEnabled = false
local chatSpamConnection
chatSpamBtn.MouseButton1Click:Connection
chatSpamBtn.MouseButton1Click:Connect(function()
	chatSpamEnabled = not chatSpamEnabled
	chatSpamBtnConnect(function()
	chatSpamEnabled = not chatSpamEnabled
	chatSpamBtn.Text = chatSpamEnabled and "💬 ON" or "💬 SPAM"
	.Text = chatSpamEnabled and "💬 ON" or "💬 SPAM"
	chatSpamBtn.BackgroundColor3 = chatSpamEnabled and Color3.fromRGB(0, chatSpamBtn.BackgroundColor3 = chatSpamEnabled and Color3.fromRGB(0, 160, 90) or Color3.fromRGB(60, 160, 90) or Color3.fromRGB(60, 60, 70)
	if chatSpamEnabled then
		chatSpamConnection = RunService.60, 70)
	if chatSpamEnabled then
		chatSpamConnection = RunHeartbeat:Connect(function()
			local chatService = game:GetService("ReplicatedService.Heartbeat:Connect(function()
			local chatService = gameStorage"):FindFirstChild("DefaultChatSystemChatEvents")
			if chatService then
				chatService.S:GetService("ReplicatedStorage"):FindFirstChild("DefaultChatSystemChatEvents")
			if chatService then
				chatayMessageRequest:FireServer("AdminService.SayMessageRequest: Panel PRO ativo!", "All")
			end
			wait(5FireServer("Admin Panel PRO ativo!", "All")
			end
			wait(5)
		end)
	else
		if chatSp)
		end)
	else
amConnection then
					if chatSpamConnection then
chatSpamConnection			chatSpamConnection:Disconnect()
			chatSpamConnection = nil
		end
	end
end)

local:Disconnect()
			chatSpamConnection = nil
		end
	end
end)

local infoCard = Instance.new infoCard = Instance.new("Frame")
infoCard.Size = UDim2.new(1, 0("Frame")
infoCard.Size = UDim2.new(1, 0, 0,, 0, 110)
infoCard.Position = UDim2.new(0, 0, 110)
infoCard.Position = UDim2.new(0, 0, 0,  0, 190)
infoCard.BackgroundColor3 = Color3.fromRGB(28,190)
infoCard.BackgroundColor3 = Color3.fromRGB(28,28,38)
infoCard28,38)
infoCard.Parent = miscPage
Instance.new("UICorner", info.Parent = miscPage
Instance.new("UICorner", infoCard).CornerRadius = UDimCard).CornerRadius = UDim.new(0, 12)
local infoStroke = Instance.new(".new(0, 12)
local infoStroke = Instance.new("UIStroke", infoUIStroke", infoCard)
infoStroke.Color = Color3.fromRGB(139, 92,Card)
infoStroke.Color = Color3.fromRGB(139, 92, 246 246)
infoStroke.Thickness = 2
infoStroke.Transparency = 0.7)
infoStroke.Thickness = 2
infoStroke.Transparency = 0.7

local infoIconLabel = Instance.new("TextLabel")
infoIconLabel.Size = U

local infoIconLabel = Instance.new("TextLabel")
infoIconLabel.Size = UDim2.new(0, 40Dim2.new(0, 40, 0, 40)
infoIconLabel.Position, 0, 40)
infoIconLabel.Position = UDim2.new(0, 12, 0, 12 = UDim2.new(0, 12, 0, 12)
infoIconLabel.BackgroundColor3 = Color3.fromRGB(139, 92)
infoIconLabel.BackgroundColor3 = Color3.fromRGB(, 246)
infoIconLabel.BackgroundTransparency = 0.8
infoIconLabel139, 92, 246)
infoIconLabel.BackgroundTransparency = 0.8.Text = "ℹ️"
infoIconLabel.Font = Enum.Font.GothamBold

infoIconLabel.Text = "ℹ️"
infoIconLabel.Font = Enum.Font.GothamBold
infoIconLabel.TextSize = 20
infoIconLabel.TextColor3 = Color3infoIconLabel.TextSize = 20
infoIconLabel.TextColor3 = Color3.fromRGB(255,255,255)
infoIconLabel.Parent = infoCard
Instance.new.fromRGB(255,255,255)
infoIconLabel.Parent("UICorner", infoIconLabel).CornerRadius = UDim.new(0,  = infoCard
Instance.new("UICorner", infoIconLabel).CornerRadius = U10)

local infoTitleLabel = Instance.new("TextLabel")
infoTitleLabel.Size = UDimDim.new(0, 10)

local infoTitleLabel = Instance.new("TextLabel")
infoTitleLabel.Size = UDim2.new(1, -70, 0, 20)
infoTitleLabel2.new(1, -70, 0, 20)
infoTitleLabel.Position = UDim2.new(0, 60, 0, 15)
infoTitleLabel.Back.Position = UDim2.new(0, 60, 0, 15)
infoTitlegroundTransparency = 1
infoTitleLabel.Font = Enum.Font.GothamBold
Label.BackgroundTransparency = 1
infoTitleLabel.Font = Enum.Font.GothamBinfoTitleLabel.TextSize = 15
infoTitleLabel.TextColor3 = Color3old
infoTitleLabel.TextSize = 15
infoTitleLabel.TextColor3 = Color3.fromRGB(255,255.fromRGB(255,255,255)
infoTitleLabel.TextXAlignment = Enum.TextXAlignment.Left
infoTitleLabel.Text = "ATALHOS DO TECLADO"
info,255)
infoTitleLabel.TextXAlignment = Enum.TextXAlignment.Left
infoTitleLabel.Text = "ATALHOS DO TECTitleLabel.Parent = infoCard

local infoText = Instance.new("TextLabel")
LADO"
infoTitleLabel.Parent = infoCard

local infoText = Instance.new("TextLabel")
infoText.Size = UDim2.new(1, -24, 0, 60)
infoText.Position = UDim2.new(infoText.Size = UDim2.new(1, -24, 0, 60)
infoText.Position = UDim20, 12, 0, 45)
infoText.BackgroundTrans.new(0, 12, 0, 45)
infoText.BackgroundTransparency = 1
infoText.Font = Enumparency = 1
infoText.Font = Enum.Font.Gotham
infoText.TextSize = 12.Font.Gotham
infoText.TextSize = 12
infoText.TextColor3 = Color
infoText.TextColor3 = Color3.fromRGB(180,180,200)
infoText.TextWrapped =3.fromRGB(180,180,200)
infoText.TextWrapped = true
infoText true
infoText.TextXAlignment = Enum.TextXAlignment.Left
infoText.TextYAlignment = Enum.TextYAlignment.T.TextXAlignment = Enum.TextXAlignment.Left
infoText.TextYAlignment = Enum.TextYAlignment.Top
infoText.Text = "• M: Abrir/Fechar Menu\n•op
infoText.Text = "• M: Abrir/Fechar Menu\n• F: Ativar/Desativar Fly\n F: Ativar/Desativar Fly\n• P: Destravar mouse\n• P: Destravar mouse\n• ESP: Liga/Desliga visualização• ESP: Liga/Desliga visualização de jogadores"
infoText.Parent = infoCard

-- ═════════════════════ de jogadores"
infoText.Parent = infoCard

-- ═══════════════════════════════════════════════════════════════
══════════════════════════════════════════
--                    EVENTOS DE ENTRADA
-- ═════--                    EVENTOS DE ENTRADA
-- ═══════════════════════════════════════════════════════════════════════════════

UserInputService.InputBegan══════════════════════════════════════════

UserInputService.InputBegan:Connect:Connect(function(input, gpe)
	if gpe then return end
	
	-- Tecl(function(input, gpe)
	if gpe then return end
	
	-- Teclas deas de bind
	if waitingForKey and not gpe then
 bind
	if waitingForKey and not gpe then
		if input.UserInputType == Enum.UserInputType.Keyboard then		if input.UserInputType == Enum.UserInputType.Keyboard then
			aimbotKey = input
			aimbotKey = input.KeyCode
			aimbotKeyName = input.KeyCode.Name
			key.KeyCode
			aimbotKeyName = input.KeyCode.Name
			keyBindBtn.Text = "⏱️ "BindBtn.Text = "⏱️ " .. aimbotKeyName
			keyBindBtn.BackgroundColor .. aimbotKeyName
			keyBindBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
			wait3 = Color3.fromRGB(60, 60, 70)
			waitingForKey = false
		elseifingForKey = false
		elseif input.UserInputType == Enum.UserInputType.M input.UserInputType == Enum.UserInputType.MouseButton1 then
			aimbotKey = Enum.UserInputType.MouseButtonouseButton1 then
			aimbotKey = Enum.UserInputType.MouseButton1
			aimbotKeyName = "Botão Esquerdo"
			keyBindBtn.Text = "1
			aimbotKeyName = "Botão Esquerdo"
			keyBind⏱️ " .. aimbotKeyName
			keyBindBtn.BackgroundColor3 = ColorBtn.Text = "⏱️ " .. aimbotKeyName
			keyBindBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
			waiting3.fromRGB(60, 60, 70)
			waitingForKey =ForKey = false
		elseif input.UserInputType == Enum.UserInputType.MouseButton2 then false
		elseif input.UserInputType == Enum.UserInputType.MouseButton2 then
			aimbotKey = Enum.UserInputType.MouseButton2
			aimbotKeyName =
			aimbotKey = Enum.UserInputType.MouseButton2
			aimbotKey "Botão Direito"
			keyBindBtn.Text = "⏱️ " ..Name = "Botão Direito"
			keyBindBtn.Text = "⏱️ " .. aimbotKeyName
			keyBindBtn.BackgroundColor3 = Color3.fromRGB( aimbotKeyName
			keyBindBtn.BackgroundColor3 = Color3.fromRGB(60,60, 60, 70)
			waitingForKey = false
		end
	else 60, 70)
			waitingForKey = false
		end
	elseif waitingForClickerKey and not gpe then
		if input.UserInputType == Enum.UserInputType.Keyif waitingForClickerKey and not gpe then
		if input.UserInputType == Enum.UserInputboard then
			autoClickerKey = input.KeyCode
			autoClickerKeyName =Type.Keyboard then
			autoClickerKey = input.KeyCode
			autoClickerKeyName = input.KeyCode.Name
			clickerKeyBtn.Text = "TECLA: input.KeyCode.Name
			clickerKeyBtn.Text = "TECLA: " .. " .. autoClickerKeyName
			clickerKeyBtn.BackgroundColor3 = Color autoClickerKeyName
			clickerKeyBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 70)
			waitingForClickerKey = false
		end
3.fromRGB(60, 60, 70)
			waitingForClickerKey = false	end
	
	-- Atalhos do teclado
	if input.KeyCode == Enum.KeyCode.M then
		end
	end
	
	-- Atalhos do teclado
	if input.KeyCode == Enum.KeyCode.M then 
		showPanel(not panelVisible)
	elseif input.KeyCode == 
		showPanel(not panelVisible)
	elseif input.KeyCode == Enum.KeyCode.F then
 Enum.KeyCode.F then
		_G.FlySystem:toggleFly()
		if fly		_G.FlySystem:toggleFly()
		if flyToggle then
			flyToggle.Text = _G.FlySystem.flying and "FLY: ON" or "FLToggle then
			flyToggle.Text = _G.FlySystem.flying and "FLYY: OFF"
			flyToggle.BackgroundColor3 = _G.FlySystem.flying and Color3.from: ON" or "FLY: OFF"
			flyToggle.BackgroundColor3 = _G.FlySystem.flying and Color3.fromRGB(0, 160, 90) or Color3.fromRGB(60,RGB(0, 160, 90) or Color3.fromRGB(60, 60,  60, 70)
		end
	elseif input.KeyCode == Enum.KeyCode.P then
70)
		end
	elseif input.KeyCode == Enum.KeyCode.P then
		UserInputService.MouseBehavior = Enum.MouseBehavior.Default
		UserInputService.MouseIconEnabled =		UserInputService.MouseBehavior = Enum.MouseBehavior.Default
		UserInputService.M true
	end
end)

-- ═════════════════════════════════════════ouseIconEnabled = true
	end
end)

-- ═══════════════════════════════════════════════════════════════
--                    EVENTOS DE JOGADORES
-- ═══════════════════════════════════
--                    EVENTOS DE JOGADORES
-- ═══════════════════════════════════════════════════════════════════════

-- Atualizar lista de jogadores
local function updatePlayersList()
	for _, btn in pairs(playerButtons) do
		if btn══════════════════════════════════════════

-- Atualizar lista de jogadores
local function updatePlayersList()
	for _, btn and btn.Parent then
			btn:Destroy()
	end
	end
	playerButtons = {}
	
	for _, player in pairs(Players:GetPlayers()) do
 in pairs(playerButtons) do
		if btn and btn.Parent then
			btn:Destroy()
	end
	end
	playerButtons = {}
	
	for _, player in pairs(Players:GetPlayers()) do
		if player ~= LocalPlayer then
			create		if player ~= LocalPlayer then
			createPlayerButton(player)
		end
	end
end

Players.PlayerAdded:PlayerButton(player)
		end
	end
end

Players.PlayerAdded:Connect(function(player)
	createConnect(function(player)
	createPlayerButton(player)
	
	player.CharacterAdded:Connect(function()
		wait(0.5)
PlayerButton(player)
	
	player.CharacterAdded:Connect(function()
		wait(0.5)
		if espEnabled then 
			updateESP() 
		end
	end)
end)

Players.PlayerRemoving:Connect(function		if espEnabled then 
			updateESP() 
		end
	end)
end)

(player)
	if playerButtons[player] then
		playerButtons[player]:Destroy()
		playerButtons[player] = nil
	end
	
	if espHighPlayers.PlayerRemoving:Connect(function(player)
	if playerButtons[player] then
		playerButtons[player]:Destroy()
		playerButtons[player] = nil
	end
	
	if espHighlights[player] then
		espHighlights[player] then
		espHighlights[player]:Destroy()
		espHighlights[player] = nil
	endlights[player]:Destroy()
		espHighlights[player] = nil
	end
	
	if espBoxes
	
	if espBoxes[player] then
		for _, line in pairs(espBoxes[player]) do
			line[player] then
		for _, line in pairs(espBoxes[player]) do
			line:Remove()
		end
		espBoxes[player] = nil
	end
end)

-- ═════════════════════════════════════:Remove()
		end
		espBoxes[player] = nil
	end
end)

-- ═══════════════════════════════════════════════════════════════
--                      LOO══════════════════════════
--                      LOOPS DE ATUALIZAÇÃO
-- ═════════════════════════════════════════════════PS DE ATUALIZAÇÃO
-- ═════════════════════════════════════════════════════════════════════════════

RunService.Heartbeat:Connect(function()
    if espEnabled then
        updateESP()
        -- Limpar highlights de jogadores que sa

RunService.Heartbeat:Connect(function()
    if espEnabled then
        updateESP()
        -- Limpar highlights de jogadores que saíramíram
        for player, highlight in pairs(espHighlights) do
            if not player or not player.Parent or not
        for player, highlight in pairs(espHighlights) do
            if not player or not player.Parent or not player.Character then
                if highlight then highlight:Destroy() end
                espHighlights player.Character then
                if highlight then highlight:Destroy() end
                espHighlights[player] = nil
            end
        end
        -- Limpar boxes de jogadores que saíram
        for player,[player] = nil
            end
        end
        -- Limpar boxes de jogadores que saí box in pairs(espBoxes) do
            if not player or not player.Parent or not player.ram
        for player, box in pairs(espBoxes) do
            if not player or not player.Parent or not playerCharacter then
                if box then
                    for _, line in pairs(box) do
                        line:Remove()
                    end
.Character then
                if box then
                    for _, line in pairs(box) do
                        line:Remove()
                    end
                end
                espBoxes[player] = nil
            end
        end
                   end
                espBoxes[player] = nil
            end
        end
    end
 end
end)

RunService.RenderStepped:Connect(function()
    updateTracend)

RunService.RenderStepped:Connect(function()
    updateTracers()
    updateNametags()
end)

-- ═════════════════════════════════════════════════════════ers()
    updateNametags()
end)

-- ═══════════════════════════════════════
--                      INICIALIZAÇÃO
-- ═══════════════════════════════════════════════════════
--                      INICIALIZAÇÃO
-- ═══════════════════════════════════════════════════════════════

-- Inicializar sistemas
_G.FlySystem:init()
setTab("PLAYERS")
updatePlayersList()

-- Mensagens de══════════════════════════════════════

-- Inicializar sistemas
_G.FlySystem:init()
setTab("PLAYERS")
updatePlayersList()

-- Mensagens de inicialização
print("✅ CASAIS SCRIPTER - Admin Panel PRO carregado!")
print("🔑 Pressione M para ab inicialização
print("✅ CASAIS SCRIPTER - Admin Panel PRO carregado!")
print("🔑 Pressione M para abrir o menu")
print("🎯rir o menu")
print("🎯 Pressione F para ativar/desativar Fly")
print("📌 Pressione P para destravar o mouse")
print("⚡ Script cri Pressione F para ativar/desativar Fly")
print("📌 Pressione P para destravar o mouse")
print("⚡ Script criado por CASAado por CASAIS SCRIPTER")
